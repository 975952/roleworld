# 项目状态 · 角色世界（RoleWorld）

> 最后更新：2026-09-10 · 当前版本 **v0.1.10**
> 仓库：https://github.com/975952/roleworld （MIT）

> 本地界面修改（2026-09-11，未发布）：新增 glass.css，统一玻璃弹窗、输入框与柔和按钮；app.js / zoom.js 同步缩放变量，容器按缩放比例补偿视口尺寸。index.html 与 magic-map.html 引入共享样式。按用户要求未运行测试，待用户直接查看。

> 侧栏角色选择（2026-09-11，已发布）：侧栏显示哪些角色改由用户自己决定 ——
> 角色库里每个角色一个「显示在侧栏」开关，选择持久化在本地偏好里；未选择的角色不进侧栏，
> 但照样能聊天、记忆与对话都不受影响。详见第八节。

> 本地修正（2026-09-11，未发布）：缩放宽度改用视口像素，修正百分比重复补偿造成的右侧空白/裁切；同步窗口变化和侧栏拖动比例。增强玻璃面板、输入框及按钮层次，模型名与价格分行，按当前名称测量选择框宽度。官方目录更新为 deepseek-flash（V4.1 Flash）与过渡期 V4 Pro；旧官方 Flash 配置读取时兼容到新名称，9 月 14 日中午起 Pro 估价跟随官方路由。来源：https://api-docs.deepseek.com/updates/ 与 https://api-docs.deepseek.com/quick_start/pricing/ 。按用户要求未测试，待用户查看。

> 本地外观调整（2026-09-11，未发布）：按用户截图移除剧情页设置带/舞台/对话流的嵌套描边，舞台改开放布局；场景与角色选中项改底色加勾选，输入字段取消轮廓线。设置页移除卡片外框与逐行分隔线，使用留白和柔和字段底色。仅修改 app/glass.css；按用户要求未运行测试。

> 本地下拉菜单调整（2026-09-11，未发布）：新增 app/choice-menu.js 并在 index.html 接入；glass.css 增加文字与箭头式选择器、圆角浮层菜单。覆盖聊天模型、设置与角色语言选择；名称完整换行，保留原 select/change 保存契约，支持方向键、Esc 和键盘查找。按用户要求未运行测试。

> 本地角色导航 A（2026-09-11，未发布）：左栏按角色分组，可折叠查看所属对话，角色行 ＋ 新建指定角色对话；移除全局新建对话按钮，新增角色管理入口。活跃对话自动展开所属角色；不同角色新会话不复用已绑定空白会话。改动 app/index.html、app/integration.js、app/app.js、app/task22-core.js、app/glass.css。使用无边框、缩进与柔和选中底色。记忆规则未改。按用户既定要求未运行测试，待用户查看。

> 本地侧栏精简（2026-09-11）：按反馈缩小角色行与对话间距，隐藏头像字块、计数、展开符号与空列表提示；桌面 ＋ 仅悬停/键盘聚焦显示，触屏保留。修改 app/glass.css；未运行测试。

---

## 一、这是什么

一个**完全本地**的 AI 角色对话应用：没有服务器、没有账号、没有遥测。
角色卡、对话记录、记忆书、API Key 全部只存在用户自己的设备上，请求从用户设备
**直接**发给用户自己配置的模型接口。

支持三件事：**角色对话**（`index.html`）、**剧情模式**（`magic-map.html`，多角色同场 + 导演模式）。
网页版与桌面版是**同一份前端代码**。

---

## 二、怎么下载桌面版

**下载页**：https://github.com/975952/roleworld/releases

每个 Release 下面有 **Assets** 区，点这个文件：

```
RoleWorld_0.1.10_x64-setup.exe      ← 就是安装包（NSIS 安装向导），1.31 MB
```

> ⚠️ Release 页面上还有两个 **Source code (zip / tar.gz)** —— 那是源码压缩包，
> **不是**安装包，别点错。要找的是 `..._x64-setup.exe`。

**安装步骤**

1. 双击 `RoleWorld_0.1.10_x64-setup.exe`；
2. Windows 会弹 **SmartScreen「Windows 已保护你的电脑」** —— 因为安装包**没有买代码签名证书**。
   点 **「更多信息」→「仍要运行」** 即可（这是正常的，不是病毒提示）；
3. 跟着向导点下去，装完开始菜单里会有「RoleWorld / 角色世界」；
4. 第一次打开会强制走三步引导，**第二步直接粘贴 API Key** → 点「保存并测试」→ 看到
   「连接正常」→ 开始用。

**为什么这么小（1.3 MB）**：Tauri 用系统自带的 WebView2 渲染，不打包 Chromium。

**不装也能用**：仓库里 `node scripts/serve.cjs` 起个本地服务，浏览器打开
`http://127.0.0.1:8080/app/index.html` 就是网页版，功能完全一样（数据存在浏览器里，
与桌面版的数据目录相互独立）。

---

## 三、现在能做什么

### 对话页
- **模型**：顶栏直接切模型；服务商支持 DeepSeek 官方 / OpenAI / OpenRouter / 硅基流动 /
  自定义（llama.cpp、Ollama、LM Studio 等任何 OpenAI 兼容端点）。Key 存本机。
- **思考模式**：默认关闭（不再先闪一段思维链再被正文顶掉），可开。
- **费用**：输入框上方实时显示本对话的 token 用量与估算费用。单价按官方价内置
  （Flash 系列闲时 ¥1/¥4、V4 Pro 闲时 ¥4.5/¥13.5），**按北京时间判断峰谷**（高峰 ×2），
  也可以在设置里自己填单价。
- **记忆**：**每个角色一套记忆**（不再是只有 Harry 有），跨对话保留；命名规则
  `MB <角色短名> — <书名>`。
- **自动记忆（agent 式）**：模型会自己把值得记住的要点写成 `[[记住: …]]`，前端剥掉标记、
  写进该角色的「自动记忆」书，常驻上下文、上限 50 条。可在设置里关。
- **角色卡**：导入 `.json` / `.png` / `.charx`；也能让 AI 帮你写卡。
- **多会话**：同一角色可以有多个对话（不同世界线），记忆共享。

### 剧情模式
- 多角色同场演出，带**点名规则**（谁在跟谁说话）与**导演模式**（玩家不在场也能推进）。
- 场景自由书写，或选通用预设，或取角色卡自带场景 —— 不绑定任何世界观。

### 外观
- 主题：暗 / 亮。
- **风格（配色）6 种**：默认（石墨）/ 羊皮纸 / 水墨 / 深林 / 樱 / **返校金**。
  对话页与剧情模式一起变。
- **亮色一律做成渐变，不整屏蒙一层**：带颜色的风格（返校金 / 樱 / 深林）只用
  "中性近黑底 + 一束淡色渐变"（顶栏/主区/侧栏/主按钮/焦点环各一道），顶部另有一条
  向下渐隐的光带，高度 `min(180px, 24vh)`；
  **深林**用很淡的浅绿（暗色下 `#b9e0a8`，亮色下底色就是很浅的浅绿 `#f3faf1`）；
  **羊皮纸**走另一条路：纸面 = 暖纸底 + 两层细纤维纹 + 暖斑 + 边缘泛黄（`glass.css` 的 paper 段）。
- **界面大小**：90%–120% 七档，5% 一档；`Ctrl/⌘` + `-` / `=` 调、`Ctrl/⌘` + `0` 复位。
- 显示密度、动效、秋季金色氛围开关。

### 数据
- 桌面版全部是**磁盘上的普通文件**，可直接备份 / 编辑 / 用 Git 管：
  `%APPDATA%\app.roleworld.desktop\data\`
  （`characters/` `chats/` `worlds/` `kv/` `blobs/`）
- 「设置 → 关于 → 导出存档 / 导入存档」用于换机器迁移。

---

## 四、技术形态

```
roleworld/
  app/                     前端（原生 HTML/CSS/JS，零构建、零依赖）
    adapter/               本地适配层
      index.js             门面：STApi 兼容形状 + RoleWorld.* 干净接口
      store.js             数据层（IndexedDB；桌面端换文件系统实现）
      desktop.js           Tauri 文件系统后端
      model.js             直连 OpenAI 兼容端点（流式返回原始 Response）
      cards.js             PNG tEXt / CHARX / CCv2 / CCv3 解包
      pricing.js           价格表 / 峰谷 / token 估算
      packs.js             内容包安装
      settings-ui.js       模型设置面板
      archive-ui.js        存档导出导入
      onboarding.js        首次引导（强制走完）
    tokens.css             全站设计变量（配色 / 风格 / 氛围）★改设计看这里
    styles.css             对话页样式
    magic-map.css          剧情模式样式（已改成与主界面同一套变量）
    zoom.js                界面大小快捷键
    task22-core.js         提示词编排（角色卡 + 记忆书 + 样例对话）
    task29-character-core.js  AI 写卡 + 角色记忆归属
  packs/harry-potter/      内置内容包（6 张 CCv3 卡 + 4 本记忆书，同人非商业）
  src-tauri/               桌面外壳（Tauri v2，8 个受限文件命令）
  scripts/                 本地服务器 / 图标 / 打包前准备 / 版本号
  tests/                   回归测试
  docs/                    PORTING.md（迁移与设计说明）/ PUBLISH.md / RELEASE_CHECKLIST.md
```

**桌面外壳刻意不用 Tauri 的 fs 插件**：那要在 capabilities 里配一长串路径通配符，
范围不好收。改成自己开 8 个命令 + 文件名白名单校验，可访问范围钉死在应用数据目录内。

---

## 五、测试与流水线

| | |
|---|---|
| `node tests/adapter-unit.cjs` | **50/50** —— 数据层、请求翻译、SSE、ZIP、价格、记忆归属、草稿解析、称呼、BOM 检查 |
| `node tests/local-app-check.cjs` | **22/22** —— 无头 Chrome 跑真实页面（合成 fixture + 假模型端点，不联网） |
| `node tests/gold-visual-check.cjs` | **75/75** —— 六个风格的外观回归 + 称呼 + 主区顶部缝隙 + 发送键形状 + 第一封信（需先起本地服务） |
| `node tests/desktop-smoke.cjs` | 1/1 —— 启动真 exe，验证数据真的落盘 |
| CI（`.github/workflows/ci.yml`） | 每次 push 在 Windows 上跑前两项 |
| Release（`.github/workflows/release.yml`） | 推 `v*` 标签 → 自动构建 NSIS 安装包并挂到 Release |

发版：`node scripts/set-version.cjs 0.1.11` → commit → `git push` → `git tag -a v0.1.11` → push 标签。

---

## 六、已知问题 / 待办

- [x] **主界面信息架构**：已改成**按角色组织** —— 左栏一级是角色，对话挂在角色下面，
      角色行右侧 `＋` 新建该角色的对话；角色名点击展开/收起，切换对话时自动展开所属角色。
      侧栏显示哪些角色由**用户自己选**（见第八节）。
- [ ] **设计**：按钮目前是统一的细边框风格，视觉上偏"通用 AI 味"，计划重做视觉
      （玻璃质感 / 更柔和的层级）。**已交给外部设计**，落地文件见上面 `tokens.css` 等。
- [ ] macOS / Linux 构建：Tauri 配置已留好，`release.yml` 里 matrix 项注释着，取消注释即可
      （macOS 正式发布需要代码签名证书）。
- [ ] PWA（可装到手机桌面）、Android（Capacitor）：未开工。
- [ ] `app.js` 里还残留少量账号时代的死代码（入口已隐藏，未物理删除）。
- [ ] 免费体验额度（送 100 条）：**未做**。纯本地应用里无法真正限制 —— Key 会随程序发出去、
      计数存在用户机器上，两条都能绕过；要真限制必须加中转服务（会破坏"无中转"的承诺）。
      目前策略是「不发 Key，只把填 Key 的过程做到最顺」。

---

## 七、服务器

旧的 SillyTavern 公网站点已停用，**服务器可以关机**：
唯一不可再生的用户数据（27 个账号 / 374 段对话 / 734 张角色卡，310 MB）已备份回
`C:\novel-llm\runs\2026-09-09-back-to-school-surprise\server-user-data-backup\`。
roleworld 完全不依赖服务器。

---

## 八、侧栏角色选择（2026-09-11，已发布）

### 行为

- **侧栏显示哪些角色，完全由用户决定**：不自动挑、不按最近使用筛、不限制数量、不自动重排。
  顺序就是你打开开关的顺序。
- 角色库（设置 → 角色管理）里每个角色一个 **「显示在侧栏」** 开关，状态存在本地偏好
  `preferences.sidebarCharacters` 里，跟着外观偏好一起持久化。
- **新导入 / 新创建的角色默认只进角色库**，不会自动出现在侧栏（要你自己打开开关）。
- 关掉开关**只影响侧栏展示**：角色卡、对话、记忆一个都不会动。
- 侧栏底部有一行低调的 **「管理角色」** 入口，进角色库调整。
- 侧栏空的时候显示一句提示 + **「选择角色」** 按钮，两种情况文案不同：
  - **从未设置** → 「还没有选择要显示在侧栏的角色。」（绝不擅自替用户挑角色）
  - **用户自己全部隐藏** → 「侧栏里还没有角色。你的角色、对话和记忆都还在，挑几个放进来就行。」
- **从角色库/角色选择器打开一个没加入侧栏的角色，照样能正常聊天**：它会临时出现在侧栏里
  承载当前对话，但**不会写进偏好**（不偷偷改变你的选择）。

### 改动文件

| 文件 | 改了什么 |
|---|---|
| `app/account-core.js` | 新增偏好字段 `sidebarCharacters`：`null` = 从未设置，数组 = 用户选择（可为空数组）。`sidebarCharactersValue()` 区分这两种状态；非数组一律按"从未设置"处理，避免脏数据被误读成"隐藏了全部" |
| `app/app.js` | 新增 `isSidebarCharacter` / `setSidebarCharacter` / `updateCharacterManageHint`；角色库每行加开关 + 顶部提示；「管理角色」入口接入导航绑定；`applyPreferences()` 结束时重刷侧栏（偏好是异步加载的，不重刷会一直停在"未设置"）；`TASK25C_UI` 导出上述三个函数 |
| `app/integration.js` | `renderChatList()` 改为按偏好过滤 + 按用户顺序渲染；当前角色临时出现（不持久化）；孤儿会话仍然展示；空状态分两种文案 + 「选择角色」入口；`renderChatList` 加入 `TASK21` 导出 |
| `app/index.html` | 侧栏 `#historyGroups` 下方加「管理角色」入口 |
| `app/glass.css` | 追加：`.sidebar-manage-characters`（低调文字入口）、`.history-empty-block` / `.history-empty-action`（空状态）、`.character-manage-hint`、`.character-manage-sidebar`（都无边框，靠留白与柔和底色分层级） |

### 测试情况

发布前已跑完整回归，**全绿**：

| 套件 | 结果 |
|---|---|
| `node tests/adapter-unit.cjs` | **44/44**（原 42；新增 2 项：旧模型别名自动迁移、官方目录只列在售型号） |
| `node tests/local-app-check.cjs` | **22/22**（无头浏览器跑真实页面） |

顺带修掉的测试与实现不一致（**不是产品 bug，是测试没跟上**）：上一轮把 DeepSeek 官方目录
从 `deepseek-v4-flash` 更新为 `deepseek-flash`，并加了旧别名自动迁移 + V4 Pro 到期后按 flash 计费，
测试里的模型名没跟着改，导致 1 项单元 + 1 项浏览器用例失败。已更新为当前目录，
并把「迁移逻辑」和「目录内容」各补了一条断言锁住。

**仍然只能靠肉眼看的**（自动化覆盖不到）：

1. 侧栏空状态与「选择角色」按钮的实际外观；
2. 角色库开关的排版与角色行是否拥挤；
3. 从角色选择器打开未加入侧栏的角色时，侧栏临时出现该角色的表现；
4. 缩放（Ctrl +/-）与新增侧栏元素的叠加效果。

> 上一轮的 `glass.css` / `choice-menu.js` 等视觉改动本轮一起发布，回归通过，但视觉细节仍需人眼确认。

---

## 九、配色与宽度返工（2026-09-11，未发布）

用户这一轮的四个现象，逐条对应到根因，都已修掉：

| 现象 | 根因 | 处理 |
|---|---|---|
| 金色"还是太浓，背景该偏黑，还不够纯金" | 表面色被染了暖色（`#1c1811` 一类），整片发闷；强调色是偏土的 `#f2c14e` | 底色改成中性近黑（画布 `#0a0a0a`，其余按灰阶递进，不带黄）；强调色改纯金 `#f5c518`，并按 `#ffe07a → #e9ad00` 做渐变 |
| "亮色应该做成渐变，而不是整个界面蒙上一层" | 氛围层是一张 `position:fixed; inset:0` 的双径向渐变，等于替全站盖了层黄膜 | 氛围层压成**顶部一条向下渐隐的光带**（高度 `min(340px, 48vh)`）；金色改落到具体表面的渐变上：顶栏、主区、侧栏、剧情模式顶栏/设置带/舞台、主按钮（`--focus-grad`）、焦点环 |
| 第一次进入勾选"保留返校金"却没显示出来 | ① 信件只切了一个 CSS class，没写进外观偏好，随后 `applyPreferences()` 按偏好（默认关）把它抹掉；② 是否保留被 `prefers-reduced-motion` 卡住——开了"减少动画"的用户永远看不到；③ 信件只给氛围、从来没给过金色配色；④ 首帧只读主题不读风格，先闪一下石墨色 | ① `TASK25C_UI` 导出 `savePreference`，信件勾选即真正落盘；② 金色是配色不是动画，去掉 `prefersReduced()` 判断（动画仍由 `data-motion="reduced"` 单独关）；③ 勾选时把风格一并设为 `gold`（用户已自选过别的配色则尊重他），取消勾选再还回去；④ `index.html` 首帧脚本改为同时落定主题 / 风格 / 氛围 |
| 窄屏时右侧空一块 | Chrome 的 `zoom` 会把 `vw` 一起缩放：`zoom:0.9` 时 `100vw` 只有 324px，`calc(100vw/0.9)` 算成 360px，壳子再被缩成 324px；同时窄屏那条 `.app-shell { max-width: 100vw }` 把补偿后的宽度又夹了回去 | 宽度改用 JS 写进来的绝对 px（`--viewport-w` / `--viewport-h`）做补偿，并覆盖窄屏的 `max-width`；`zoom.js` 与 `app.js` 都写这两个变量 |
| 有时屏幕上什么都没有 | `theme-pending` 期间 `.app-shell` 是 `visibility: hidden`，启动链路上任何一点异常都会留下整屏空白 | 只关过渡动画，不再隐藏界面；看门狗兜底从 20s 收到 3s |

### 改动文件

| 文件 | 改了什么 |
|---|---|
| `app/tokens.css` | 返校金暗/亮两套色板改成中性近黑 + 纯金；新增 `--focus-grad`；氛围层由整屏双径向改成顶部渐隐光带 |
| `app/glass.css` | `#appShell` 宽度补偿改用 `--viewport-w/--viewport-h` 并取消 `max-width` 夹取；新增 `data-style="gold"` 段（顶栏/主区/侧栏/剧情模式表面/主按钮/焦点环的金色渐变） |
| `app/styles.css` | `theme-pending` 不再隐藏界面 |
| `app/index.html` | 首帧脚本同时落定主题、风格、氛围；看门狗 3s |
| `app/seasonal-surprise.js` | 信件勾选/取消即时生效并写入外观偏好；取消勾选会把风格还回原样；不再受 `prefers-reduced-motion` 影响 |
| `app/app.js` | `TASK25C_UI` 导出 `savePreference`；`updateVisualViewportMetrics()` 额外写 `--viewport-w/--viewport-h` |
| `app/zoom.js` | `syncViewport()` 同步写 `--viewport-w/--viewport-h` |
| `tests/gold-visual-check.cjs` | 新增：32 项外观回归（配色、渐变、第一封信保留/取消、窄屏 1/0.9/1.2 三档宽度、灰屏兜底） |

### 测试情况

| 套件 | 结果 |
|---|---|
| `node tests/adapter-unit.cjs` | **46/46** |
| `node tests/local-app-check.cjs` | **22/22** |
| `node tests/gold-visual-check.cjs` | **32/32** |

**还需要人眼确认的**：金色渐变的浓度是否合适（这是主观项，数值上已经是"近黑底 + 低透明度渐变"）。

---

## 十、四个风格返工（2026-09-11，未发布）

用户第二轮反馈："还要更淡一点、光晕更小一点，樱也要和这个差不多，重做羊皮纸还有绿色，
羊皮纸质感做好，深林要很淡的浅绿底色，很浅很浅。"

| 风格 | 改了什么 |
|---|---|
| 返校金 | 光带高度 340px → **180px**（`min(180px, 24vh)`），不透明度 .8 → .55，渐变色从写死的金色改成取 `--focus`；表面渐变整体减半（顶栏 8%→5%，主区 13%→7%，侧栏 7%→4%）；`--accent-soft` .14→.10 |
| 樱 | 与返校金同一套做法：底色由粉紫染底改成**中性近黑** `#0b0a0b`，只保留粉色强调 `#ef9ec4` + 粉色渐变 `#ffc2dc → #e483ae`；光带同金 |
| 深林 | 不再是浓墨绿：暗色底色近黑 `#0a0c0a`，绿色一律用**很淡的浅绿** `#b9e0a8`；亮色底色就是**很浅很浅的浅绿** `#f3faf1` |
| 羊皮纸 | 亮色纸底改成 `#f6efdf`（更暖、不再发土）；新增真正的纸**质感**：两层 1px 斜向纤维纹 + 两团暖斑 + 一圈边缘泛黄，叠在纸底上；纸面下的输入框/弹窗改成不透明纸面，不再用玻璃拟态 |

### 改动文件

| 文件 | 改了什么 |
|---|---|
| `app/tokens.css` | 四套风格色板重写；每套补 `--focus-grad`；顶部光带改小改淡并统一取 `--focus`（金/樱各自成色）；字体抽成 `--font-family` |
| `app/glass.css` | 金色段扩成"带颜色的风格"段；新增羊皮纸纸纹层 |
| `tests/gold-visual-check.cjs` | 32 → **45** 项：新增樱（近黑底/粉渐变/光带尺寸）、深林（亮色浅绿底 + 暗色近黑不染绿）、羊皮纸（两种主题下都真有纤维纹理 + 渐变按钮） |

### 测试情况

| 套件 | 结果 |
|---|---|
| `node tests/adapter-unit.cjs` | **46/46** |
| `node tests/local-app-check.cjs` | **22/22** |
| `node tests/gold-visual-check.cjs` | **45/45** |

---

## 十一、风格再返工 + 称呼（2026-09-11，未发布）

用户反馈："羊皮纸做得更像返校金，现在返校金替换掉，重新设计羊皮纸，可以改变界面样式，
要就是羊皮纸；绿色不做渐变，做得更浅；粉色要更浅，药片更粉；现在的颜色都太暗了，
色彩要鲜艳但亮度不高。另外第一次登录让用户输入自己的名字称呼，以后在设置里能改。"

### 风格

| 风格 | 这一版 |
|---|---|
| **返校金** | 接管上一版羊皮纸那套"暖金属底"：暖金底 + 金箔纤维纹理（两层 1px 金色斜纹）+ 边缘暖褐，纯金强调色不变 |
| **羊皮纸** | 不再是"另一种配色"，而是**另一套界面**：整页衬线体（`--font-family`）、圆角收到 3px、去掉全部玻璃拟态与模糊（输入框/弹窗/侧栏都是实心纸面）、按钮改成实心墨印；底色两种主题下都是纸（亮 `#f2e7cb` / 暗 `#ded0ad`），文字是墨（`#2b2110` / `#322614`）；纸纹 = 两层棕色纤维 + 纸浆不匀 + 边缘泛黄 + 正文区极淡横纹 |
| **深林** | **一处渐变都不加**：顶部光带、主区 radial、侧栏 color-mix、按钮渐变全部按回平涂（`--focus-grad` 直接是纯色）；绿色更浅（暗 `#a7e08f`，亮底 `#f4fbf1`） |
| **樱** | 粉色更浅更纯（`#ffa6cd`，亮色 `#e2559a`）；药片/小按钮由 20% 提到 **28% 粉底 + 45% 粉字**，选中态 44%/55% —— "块越小越敢上色"，整片面积仍然干净 |

### 称呼（第一次登录填，之后可改）

- **引导多了一步**：`欢迎 → 怎么称呼你 → API Key → 就绪`。输入框默认值是「我」，回车即下一步，
  写不写都走得下去（留空存「我」）。存进本账号外观偏好 `preferences.nickname`。
- **设置 → 关于 → 称呼**：一个输入框，失焦或回车保存，改完立刻生效（走 `applyPreferences()`）。
- 生效范围：顶栏/菜单/设置的显示名、对话气泡的用户名、发给模型的 `user_name`、
  剧情模式的"你（称呼）"与说话人、返校季信件的「致 X」。
- 约束：去首尾空白与控制字符、最长 24 字符（超长截断不丢弃）、非字符串一律当未设置。
- 剧情模式页不加载 `app.js`，所以它自己从偏好里读 `nickname`（`readNickname()`）。

### 改动文件

| 文件 | 改了什么 |
|---|---|
| `app/tokens.css` | 金/纸/林/樱四套色板重写；`--font-family` 变量；深林与羊皮纸的 `--focus-grad` 改成纯色（=平涂）；光带名单里去掉深林 |
| `app/glass.css` | 深林"全平涂"段；返校金金箔纹理段；羊皮纸换皮肤段（衬线圆角墨印、去玻璃模糊）；樱药片变粉段 |
| `app/account-core.js` | 偏好新增 `nickname`；`nicknameValue()` 清洗 + `NICKNAME_MAX=24`；导出 |
| `app/adapter/onboarding.js` | 新增「怎么称呼你」步骤、读写称呼（优先 `TASK25C_UI.savePreference`，否则直写偏好）、回车下一步 |
| `app/app.js` | `currentNickname()` / `setNickname()`；显示名让位给称呼；设置里称呼输入框接线；监听 `roleworld:nickname-changed`；`TASK25C_UI` 导出 |
| `app/index.html` | 设置 → 关于 新增称呼输入框 |
| `app/integration.js` / `app/magic-map.js` | 对话与剧情模式都优先使用称呼 |
| `tests/adapter-unit.cjs` | 46 → **50**：称呼默认值、清洗、超长截断、存取往返 |
| `tests/local-app-check.cjs` | 引导用例跟着新步骤走，并断言称呼落盘 + 设置里有输入框 |
| `tests/gold-visual-check.cjs` | 45 → **62**：樱药片粉度、深林"零渐变"、羊皮纸衬线/直角/墨印/纸底/对比度、返校金金箔、称呼在两页生效 |

### 测试情况

| 套件 | 结果 |
|---|---|
| `node tests/adapter-unit.cjs` | **50/50** |
| `node tests/local-app-check.cjs` | **22/22** |
| `node tests/gold-visual-check.cjs` | **62/62** |

> 羊皮纸的对比度已用自动化量过：侧栏文字在纸底上 8.4（暗）/ 11.6（亮），都在可读线以上。
> 仍然只能靠肉眼的是：金箔纹理与纸纹在真实显示器上的观感浓淡。

---

## 十二、主区顶部缝隙 + 樱再淡一档（2026-09-11，未发布）

用户："就把樱再弄淡一点，然后聊天界面右边的主区上面有一个比较大的缝隙，需要减小，
上面最多留出 0.几毫米的小缝隙。"

| 项目 | 改动 |
|---|---|
| **主区顶部缝隙** | 根因是 `styles.css` 里 `.main-stage { margin-top: 8px }`（100% 缩放下约 2.1mm）。改成 **2px**（≈0.5mm）。窄屏那条本来就是 0，不受影响。自动化实测：宽屏 2px、窄屏 0px |
| **樱再淡一档** | 粉再提浅一档（暗 `#ffa6cd` → `#ffb6d6`，亮 `#e2559a` → `#e268a2`）；药片从 28% 收到 **22% 粉底 / 38% 粉字**（悬停 32%，选中 36%/48%）；`--accent-soft` .16 → .12（亮 .14 → .10）；顶部光带单独收到 .38（亮 .3），是六个风格里最轻的一个 |

### 追加：樱"粉的面积变小 + 很浅"（同日第三次调整）

用户："粉色太浓了，区域要变小，而且要变很浅。"

| 项目 | 改动 |
|---|---|
| 粉的面积 | 把上一版"所有药片都上粉底"整段**删掉** —— 普通按钮、剧情模式按钮、角色药片、场景按钮全部回到中性底色；粉只留在**选中的那一块**（很小的面积：选中导航项/会话、选中的角色药片、场景"我在场"开启态，10% 底色） |
| 粉的浓度 | 强调色提到接近白（暗 `#ffc2da`）；主按钮不再是实粉块，改成**很浅的粉底 + 深梅色字**（`#ffe9f3 → #ffd3e4` / 亮 `#ffdcec → #ffc4de`）；`--accent-soft` .08（亮 .07） |
| 顶部光带 | 樱单独缩小到 `min(120px, 16vh)`、透明度 .26（亮 .2） |
| 粉不偏红 | 用户："这个粉色太偏红了"。旧的 `#ffc2da` 色相约 **336°**（红=0/360°，越靠近 0 越红），红绿差 61；换成 `#f7c9e4`（色相 **325°**、红绿差 46，并带一点蓝），按钮渐变同步换成 `#ffeaf6 → #f9d3ea`，亮色 `#db7fb4` / `#ffdcef → #f7c2e0`。回归里加了色相断言（318–330°），偏红就红 |
| 粉再收到 2/5 | 用户："还要变浅到现在的 2/5"。把"粉的偏离量"整体乘 2/5：暗色强调色 `#f7c9e4`（红绿差 46 / 蓝绿差 27）→ **`#f7e5ef`（18 / 11）**；亮色 `#db7fb4`（92 / 53）→ **`#dbb6cb`（37 / 21）**；按钮渐变 `#ffeaf6→#f9d3ea` → `#fff7fc→#f9eaf3`（亮 `#fff1f9→#f7e2ee`）；选中底色 10% → **4%**；顶部光带 .26 → **.10**（亮 .2 → .08）；`--accent-soft` .08 → **.03**（亮 .07 → .028）。顺手删掉樱里"把设置计数染成粉字"那条（既多一块粉，浅色主题下也读不清） |
| 整屏不泛粉 | 用户："现在太粉了，一看整个界面都泛着粉色，看着恶心。" 根因是**大面上色**，不是按钮：① `glass.css` 里通用规则把侧栏调成 `color-mix(--navigation, --focus)`、主区加了两团 `--focus` 光晕；② 面板灰本身带紫粉（`#141116` / `#f9e7f0` 一类）；③ 樱还有一整条整屏宽的粉光带；④ `styles.css` 里还留着一层 `html.season-ambient-on::after`（`inset:0` 的整屏金色光点膜）。处理：面板色全部改**纯中性灰**、侧栏/主区/舞台/顶栏按回平涂、樱取消整屏光带（并让氛围开关在樱/深林/羊皮纸上不再生成这层），**删掉那层遗留的整屏氛围膜**（"整个界面蒙一层"最早的来源）。粉只剩：主按钮、选中项 4% 底、焦点环、勾选框 |
| 粉放到按钮上 | 中间用户又试了两句："这是一点颜色也没有了，侧边栏的少加一点，主界面少少加一点"（试了一版侧栏 7% / 主区 3% 的薄洗色），最后定调：**"不要整栏变粉，做成按钮有些按钮变粉"**。最终形态：侧栏 / 主区 / 顶栏全部回到中性平涂（一点粉都不加）；**动作按钮变粉** —— `.send-button`（发送）、`.new-conversation-button`（新建对话）、`.primary-button`、剧情模式主按钮、返校季信件按钮，粉是明确的樱粉 `#ffb3d6 → #ff8cc4`（亮 `#ff9ecb → #f06aab`）+ 深梅色字；**普通/次要/图标/危险按钮保持中性**，层级才分得开 |
| 发送按钮降粉 | 用户："那个发送按钮太粉了，逆天。" 发送按钮是圆形小按钮、整天杵在输入框边，跟主按钮同款实粉很吵。单独给它换成**很浅的粉** `#ffe3f0 → #ffd2e4`（红绿差 28，主按钮是 76），阴影也从 18% 收到 12% —— 它仍然在"粉按钮"这一组里，但明显比主按钮淡一档 |
| 发送键形状 | 用户："发送箭头太丑了，把键帽做长一点，现在像一根签子" → 先试了胶囊形 + CSS 三角箭头，用户否掉（"真他妈的丑"）→ 最终：**圆形按钮 34px** + **SVG 线宽箭头**（`fill:none`、`stroke:currentColor`、`stroke-width:2.6`、圆头圆角，一条粗细不变的线上箭头）。同时揪出真 bug：`integration.js` 每次刷新会把发送键 innerHTML 重写成 `<span>↑</span>`，所以只改 HTML 没用 —— 两处已同步 |
| 粉按钮再浅一半 | 用户："粉色的版本按钮颜色改到现在的 1/2 浅"。动作按钮渐变按"粉的偏离量 ×1/2"缩：暗色 `#ffb3d6→#ff8cc4` → **`#ffd9ea→#ffc6e2`**（红绿差 76 → 38）；亮色 `#ff9ecb→#f06aab` → **`#ffcfe6→#f0adcd`**（97 → 48）；发送键 `#ffe3f0→#ffd2e4` → **`#fff1f7→#ffe9f2`**（28 → 14） |

改动文件：`app/styles.css`（主区 margin-top）、`app/tokens.css`（樱色板）、`app/glass.css`（樱药片与光带）、
`tests/gold-visual-check.cjs`（新增"主区顶部只剩发丝缝 ≤2px"两项，并把首条内容距主区顶的距离打进结果里：
宽屏 122px = 60px 顶栏 + 32px 内容上留白 + 30px 日期分隔；窄屏 76px）。

测试：`adapter-unit` **50/50**、`local-app-check` **22/22**、`gold-visual-check` **64/64**。
