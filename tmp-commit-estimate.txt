feat(cost): 发送前预估 + 输出上限与上下文分开（P2-1 / P2-2）

P2-1 发送前预估：输入框上方多一行
  这次约 12.3k 输入 ≈ ¥0.0123（命中缓存 ≈ ¥0.0021） · 输出上限 32.8k
估法用**上一轮的真实用量**（接口回 usage 就是真值）+ 它上一句回复（下一轮会进历史）
+ 输入框里的草稿。不额外发请求，也不重拼一遍提示词（拼出来只是另一个近似值）。
打字即更新（250ms 防抖）；接口没回 usage 时按字数估，并在悬停里明说"只能看量级"。

P2-2 上下文与输出上限分开：
- task22-core 新增 contextLimitFor / outputLimitFor / checkContextBudget
  （本地上下文 32k、输出 2048；DeepSeek 上下文 1M、输出 32768）；
- 「本次请求」面板多一行把两者分开写清楚，并给出输入占上下文的百分比；
- 发送前检查：输入 + 输出上限超过上下文时**直接不发**，写明该改什么
  （调小历史预算 / 减少记忆条数 / 换模型 / 新开一段对话），
  而不是等接口回 400 再让用户猜。

顺带修了测试端的盲点：假模型端点从来不回 usage，"按真实用量估算"这条路径
在浏览器用例里从没被跑过。现在假端点会像真实接口一样在最后回一段用量
（含缓存命中数），台账与预估都按真实值走。

npm test 全绿：80 / 31 / 15 / 13 / 27 / 55 / 30 / 11。
