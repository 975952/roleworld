# roleworld relay 的环境变量（复制成 .env 再填，别提交进仓库）
#
# 上游：默认 DeepSeek 官方。想用别的服务商就改这里。
UPSTREAM_BASE=https://api.deepseek.com
# 真正的 API Key —— 只存在服务端，绝不下发给同学。
UPSTREAM_KEY=sk-换成你自己的
# 发卡/停卡口令：自己随便定一串长随机字符。**不设就等于关闭管理接口。**
ADMIN_SECRET=换成一串长随机口令
# 账本：memory（重启即丢）/ file（要配 CARD_FILE）/ cloudbase（云开发数据库）
CARD_STORE=file
CARD_FILE=/data/cards.json
# 只允许这些模型（逗号分隔；留空 = 不限制）
ALLOW_MODELS=deepseek-flash
PORT=8787
