# roleworld relay 的环境变量（复制成 relay/env 再填；别提交进仓库）
#
# 上游有两种接法，二选一：
#
# 【A】自己的服务商（默认 DeepSeek 官方）
UPSTREAM_BASE=https://api.deepseek.com
UPSTREAM_CHAT_PATH=/v1/chat/completions
# UPSTREAM_KEY=sk-你自己的（填在云托管的环境变量里，别写进仓库）
#
# 【B】云开发自己的大模型网关（一个账单、走套餐资源点）
#   控制台「AI 工具中使用 Token」页可以创建 API Key；Base URL 形如
#   https://<envId>.api.tcloudbasegateway.com/v1/ai/cloudbase
#   官方文档：https://docs.cloudbase.net/http-api/ai-model/call-llm
#   注意聊天路径是 <base>/chat/completions（不带 /v1）；
#   流式必须在请求头带 Accept: text/event-stream —— 中转已经自动带了。
# UPSTREAM_BASE=https://cyan1-d2gpky2z903b86182.api.tcloudbasegateway.com/v1/ai/cloudbase
# UPSTREAM_CHAT_PATH=/chat/completions

# 发卡/停卡口令：自己随便定一串长随机字符。**不设就等于关闭管理接口。**
ADMIN_SECRET=换成一串长随机口令
# 账本：memory（重启即丢）/ file（要配 CARD_FILE；容器里是临时盘）/ cloudbase（云开发数据库，推荐）
CARD_STORE=file
CARD_FILE=/data/cards.json
# 只允许这些模型（逗号分隔；留空 = 不限制。用云开发网关时先留空，确认模型名后再收紧）
ALLOW_MODELS=deepseek-flash
PORT=8787
