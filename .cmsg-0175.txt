release: 0.1.75 —— 修掉 CI 上那条写死时区的断言（功能与 0.1.74 相同）

- `tests/adapter-unit.cjs` 里"消息时间"那条断言原本写死 `[09-18 14:32]`，
  而时间前缀按**本机时区**算、GitHub runner 跑在 UTC（实测 06:32）——
  写死时区 = 在别的机器上必红的假失败。现在只钉格式 + 按本机时区现算，正文一字不改。
- `historyTimeLine()` 补一句注释：刻意不声明"今天是几号"，免得按机器算的示例把模型带偏。
- 撤掉临时诊断工作流（用它抓到上面那条断言的真实输出，已删）。

npm test 全绿（本地与 `TZ=UTC` 各一遍）：LOCAL_APP 129/129、VIEWPORT 75/75、ADAPTER 119/119。
网站已部署并逐字节核验（86/86、线上 version.json = 0.1.75）。
