# 夜间工作清单 · 2026-09-11 夜

> 给用户的说明：这是今晚（你睡觉期间）我要做完的清单。
> 每完成一项，我会在 `docs/NIGHT_LOG_2026-09-11.md` 里追加一条记录（做了什么、测试结果、commit）。
> **我不会在夜间自动上线**（不部署、不推 `main`、不动任何凭据）——所有改动只提交到 `codex/web-deploy` 分支，
> 你早上看过之后再决定要不要发。

---

## 硬性边界（今晚不碰）

| 不做 | 原因 |
|---|---|
| `tcb hosting deploy`（发布上线） | 上线要你确认，尤其今晚会改 UI |
| 推 `main` 分支 / 打 tag / 出安装包 | 会触发桌面端 Release 流水线 |
| 任何 API Key、账号、凭据 | 项目隐私边界 |
| CloudBase 控制台配置、免费版提示页 | 那是平台行为，不是我们的页面 |
| 跨设备同步、语音 | 需要你先做架构/隐私决定 |
| 主动提醒/召回类功能 | 你定过底线：默认关闭、不做冷落机制 |

每完成一项的标准：**5 个测试套件全绿**（adapter-unit / memory-unit / search-unit / metrics-unit / inventory-probe / local-app-check），并单独提交。

---

## P0 · 先把现在的状态钉死（先把底盘验完，再往上加）

| # | 任务 | 做法 | 完成标准 |
|---|---|---|---|
| P0-1 | **多视口回归** | 新增/扩展无头浏览器用例：1280×900、1024×768、768×1024、390×844、320×568。检查：无横向溢出、新增的「本次请求」弹层与「角色记忆」面板在窄屏可用、「记错/编造」按钮在触屏可点（≥44px 命中区） | 全部视口通过，任何溢出/裁切都算失败并修掉 |
| P0-2 | **存档往返完整性** | 单测 + 浏览器用例：造含「来源元数据的记忆 / 长对话 / 多角色 / 头像」的库 → 导出 → 导入到空库 → 逐字段比对 | 导出导入后**记忆来源、主题、替换记录、对话、记忆书一字不差**；存档里不含 API Key |
| P0-3 | **长对话压力** | 合成 800 轮对话（约 20 万 token）跑一遍：发送、检索、台账、面板渲染 | 不崩、不卡死；检索延迟 < 1s；面板能正常显示；明确记录实测延迟 |
| P0-4 | **隐私出口自查** | 逐个断言"哪些内容会离开设备"：只有发给模型服务商的那一份请求体 | 写成一个自动化用例：断言请求体里**不含** API Key、不含其他角色的对话与记忆、不含 `localStorage`/`kv` 里的偏好 |

## P1 · 阶段③ 剩下的（记忆）

| # | 任务 | 做法 | 完成标准 |
|---|---|---|---|
| P1-1 | **记忆整理** | 记忆面板：按主题分组显示、单条清空、一键清空该角色全部记忆（二次确认） | 清空后下一轮请求里确实一条不剩 |
| P1-2 | **记忆条数上限可调** | 现在写死 50 条。改成设置里可调（默认 50），并在面板显示"已用 N/上限" | 改设置后生效，超出时挤出最旧的并提示 |
| P1-3 | **来源可追溯** | 记忆面板里点来源，能跳到那句原话（若那段对话还在） | 点得动、跳得对；对话已删时给出明确说明而不是报错 |

## P2 · 阶段④ 剩下的（上下文与检索）

| # | 任务 | 做法 | 完成标准 |
|---|---|---|---|
| P2-1 | **发送前预估** | 发送前显示这次请求的**预估输入 token 与预估费用**（含缓存假设），与上一轮实际值对照 | 预估与实际相差在合理区间（写进用例）；不发起额外请求 |
| P2-2 | **输出上限与上下文分开处理** | 现在 `max_tokens` 与上下文混在一起看。拆开：输出上限按模型目录给建议值，超限在**发送前**提示 | 超限时给可操作提示，而不是发完只弹"请重试" |
| P2-3 | **检索结果引用** | 模型引用历史时，回复下方标出"引用了哪几条记录"（可点开看原话） | 有引用的轮次能看到出处；没引用不显示 |

## P3 · 阶段⑤ 及其它

| # | 任务 | 做法 | 完成标准 |
|---|---|---|---|
| P3-1 | **PWA（可装到桌面/手机）** | manifest + service worker：缓存静态资源、离线可开壳、更新时提示刷新 | 断网后仍能打开界面并看到已有数据；有新版时提示刷新 |
| P3-2 | **主动提醒：确认不存在** | 断言代码里没有任何"定时唤起用户"的机制（无冷落惩罚、无内疚话术） | 写成用例：检索代码中不存在相关定时器/文案 |
| P3-3 | **后台状态页** | `about.html` 扩成"机器可读"的状态页：版本号、测试口径、数据边界、已知问题 | 内容与当前代码一致，且列出"夜里做了哪些事" |

## P4 · 文档（全部落盘，便于你早上快速看）

| # | 任务 | 产出 |
|---|---|---|
| P4-1 | **故障排查手册** | `docs/TROUBLESHOOTING.md`：免费版提示页怎么理解、Key 无效/限流/超限怎么分辨、数据在哪、怎么备份、怎么回退版本 |
| P4-2 | **五阶段验收清单** | `docs/ACCEPTANCE.md`：把你最初那张阶段表逐条落成"可勾选清单"，每条写清当前状态与依据（测试名或实测） |
| P4-3 | **数据与隐私说明（面向用户）** | 更新 `about.html` 与 README：明确"本地保存不等于不上云"、哪些内容会离开设备 |
| P4-4 | **夜间工作日志** | `docs/NIGHT_LOG_2026-09-11.md`：逐条记录今晚每一项的结论与 commit |

---

## 优先级与取舍

时间够就按 **P0 → P1 → P2 → P3 → P4** 顺序做；做不完就停在完整的一项上，**不留半成品**。

如果中途遇到需要你决定的事（例如某个设计要改文案、某个功能要取舍），我不会替你决定：
会把它写进 `NIGHT_LOG` 的「待你决定」一节，并跳过该项去做下一项。

## 早上你要验收的东西

1. 打开本文件旁边的 `docs/NIGHT_LOG_2026-09-11.md`，看今晚实际做完了哪些；
2. 跑一次 `npm test`（应当全绿）；
3. 用真实浏览器打开线上站点，确认那个免费版提示页点掉之后能进应用；
4. 决定要不要把今晚的改动发上线（我会等你确认）。
