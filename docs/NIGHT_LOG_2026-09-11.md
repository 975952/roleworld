# 夜间工作日志 · 2026-09-11 夜

> 对应清单：`docs/NIGHT_WORKLIST_2026-09-11.md`
> 规则：每项完成即追加一条；测试全绿才提交；**不部署、不推 main、不碰凭据**。
>
> **更新（2026-09-12 凌晨）：用户早上明确指示「全部上线，然后继续开发」，
> 因此本文件里"不部署"那条边界已在用户授权后解除** —— 部署记录见文末「上线记录」。

---

## 第二轮开发：模式参数分层 · 记忆取向 · 截断提示（0.1.14 → 0.1.15）

用户先问"不同模式的哪些参数是否需要不同"，讨论后拍板：**按我改后的建议来** ——
不按模式硬压输出长度（剧情页/伴侣都靠提示词约束 + 记录实测），但要补上"截断提示 + 接着说"。

### ① 用途档案（purpose）：把"模式差异"从默认结果变成显式配置

以前这些差异是**没人管**的：剧情模式页一直吃着"输出上限 32768"（它只要一句台词），
而它又没有对话页那条格式指令，全靠各自绕开。现在核心层多了一张表：

| 用途 | 谁用 | 温度 | 回复格式 | 自动检索 |
|---|---|---|---|---|
| `chat` | 对话页 | 0.8 | 旁白/台词双引号 | 开 |
| `companion` | 对话页 + 伴侣模式 | 0.85 | 同上 | 开 |
| `scene` | 剧情模式页 | 0.95 | **只写自己这一小段、不替别人说话、不复述** | **关**（它有自己的场景历史） |

- 数字**只在这张表里出现一处**（`PURPOSE_PROFILES`），页面只表达"我是哪种用途"；
  覆盖也受白名单约束（表里没有的键塞不进来）—— 这正是 v0.1.3"两套配置打架"那次的教训；
- **输出上限刻意不按模式硬压**：表里留了 `maxOutput` 这个口子，等实测数据说话；
- `describeRequest`（「本次请求」面板）也跟着用途走，面板里显示的就是真正发出去的那条格式指令。

### ② 撞上输出上限：明确提示 + 「接着说」

以前完全不看 `finish_reason`：用户只看到一句半截的话，不知道是模型写崩了还是被砍了。
现在流式与非流式两条路径都会读 `finish_reason`，撞上 `length` 时：

- 在那一轮下面标一行「说到一半被截断了（撞上了输出上限）」，并给一个**「接着说」**按钮；
- 点它发出的是一句**看得见**的舞台提示（`（接着你刚才没说完的那句继续说…）`），
  不是偷偷替用户说话；
- 标记写进消息的 `extra`，**刷新之后仍然在**；
- 台账里记下"这一轮被截断了"，并按用途分开统计平均输出长度 ——
  "到底该不该限制长度"以后有数据可看，而不是我凭感觉定 300。

### ③ 记忆取向（P5-3）

记忆面板顶部多了一行「记忆取向」，按角色存：

- **平衡**（默认）：只记你的事实，剧情一律不进（原有底线不变）；
- **陪伴**：上限满时先挤剧情、再挤"说不清主题的碎事件"，最后才动
  「关于你这个人」的稳定事实（称呼/住处/年龄/工作/约定/怕的东西/饮料/食物）；
- **剧情**：剧情可以记，但**标明是剧情** —— 注入时写成 `[剧情] …`，面板里也标出来，
  而且它是最先被挤掉的（存在，但绝不冒充你的事实）。

另外加了「**说得对**」：点过的记忆在挤占时最后才动（改口替换后标记仍然跟着走）。

### 写这一轮时抓到的三个真问题

1. **归一化丢字段**：`normalizeSource` 只保留它认识的键，我新加的 `kind` / `confirmed`
   被悄悄丢掉 —— 结果是"剧情被当成事实""确认过的照样被挤掉"。用例当场抓住，已把这两个标记
   纳入归一化（并补了注释说明为什么不能丢）。
2. **挤占顺序第一版太粗**：我原以为"没主题的"会先被挤，但 `inferTopic` 对"考拉"这种短名词
   也会给出主题 —— 于是"陪伴取向"和"平衡取向"行为几乎一样。改成真正的区分：
   陪伴取向下按"主题是否属于关于你这个人的那几类"排序，平衡取向只把剧情提前、其余按新旧。
3. **无头环境点错按钮会挂死**：新测试第一次跑就卡住不动 —— 我点的"第一个 `.memory-actions` 按钮"
   其实是**组标题**上的「清空整组」，它弹 `confirm()`，而无头环境里没人点确定。
   给行内按钮加了独立类名 `memory-confirm`，测试只点它。

**测试**：adapter-unit 86 → 91、memory-unit 31 → 38、metrics-unit 13 → 15、local-app-check 56 → 59；
九个套件合计 **311** 项全绿。


---

## 上线记录（2026-09-12 凌晨，用户授权后执行）

- 命令：`node scripts/prepare-desktop.cjs` → `tcb hosting deploy app -e cyan1-d2gpky2z903b86182 --verify --safe`；
- 结果：远端 56 个文件（其中 6 个是平台自带的 `__auth/*`、`cloud-admin/index.html`）；
- **核对方式**：把本地 `app/` 下 **50 个文件逐个与线上对比字节数**（带 `Cache-Control: no-cache` 绕开 CDN）——
  **50/50 全部一致，没有差异、没有取不到的**；重点文件 `index.html`、`sw.js`、`manifest.webmanifest`、
  `version.json`、`pwa.js`、`icons/icon-192.png`、`icons/icon-512.png`、`about.html`、`companion-core.js` 全部 200；
- 站点：<https://cyan1-d2gpky2z903b86182-1485756522.tcloudbaseapp.com>
- 边界：**没有推 GitHub、没有推 `main`、没有动桌面端 Release**；提交仍只在本机 `codex/web-deploy` 分支；
- 还需你做：在真实浏览器里点一次（CloudBase 边缘防护会拦无头浏览器，自动化验收在这条链路上不可用）。
  建议顺手试两件事：地址栏里的「安装」图标、以及拔网后刷新是否还能打开。

**后续几次发布（同一段记录往下续）：**

- **0.1.16**（删账号死代码 + 生成参数面板）与 **0.1.17**（「记忆」栏改显示真实记忆书）：同样用
  `tcb hosting deploy app --verify --safe` 发布，并逐文件对过字节（0.1.17 时 50/50 一致）；
- **0.1.18**（顶栏「记忆」按钮点不到，见第五轮）：2026-09-12 中午发布。
  这次把"核对"写成了脚本：`node scripts/verify-live.cjs` —— 把本地 `app/` 下 50 个文件逐个取回、
  比字节数 + sha256，另外钉三件事：线上 `version.json` 必须等于仓库版本、
  线上 `index.html` 必须有顶栏「记忆」按钮**且不许带 hidden**、必须引用 PWA 清单。
  结果：`✓ 50/50 个文件与线上逐字节一致；version.json = 0.1.18；顶栏「记忆」按钮在线且没带 hidden`。
- 这次发布中途踩到一次 **CloudBase CLI 登录过期**：命令自己弹了授权页（`tcb.cloud.tencent.com/...cli-auth?user_code=…`），
  第一遍在授权完成前就退出了（`No valid identity information`），重跑一次才成功。
  以后再遇到弹授权页，是**需要人点一下同意**的，不是我这边能代劳的。


---

## 第三轮开发：删账号死代码 + 生成参数面板（0.1.15 → 0.1.16）

用户当面的第二项要求：「清理账号死代码、生成参数面板」。

### ① 删掉账号死代码（约 700 行）

产品里早就没有账号了（本地档案：没有密码、不同步），但注销/改密/改名/管理员账号列表
那一整套对话框、处理函数和状态字段还留在代码里，只是入口被 `hidden` 起来。

- `app/app.js` 删 585 行：改名、改密、注销账户、管理员删除/新建账号、账号列表（含提升/降级/停用）、
  退出登录，以及它们各自的键盘处理与焦点管理；
- `app/index.html` 删 94 行：5 个账号对话框、用户菜单里的改名/改密/退出登录、设置里的两行、
  注销卡片、整个「账号管理」设置区与导航项；
- `app/account-core.js`：删掉只服务于它们的两个函数，并改掉文件头注释
  （它现在是"本地档案 + 界面偏好存取"，不再是账号契约）。

**保留**：本地档案读取、偏好读写/重置、本地数据清理、通用 AI 数据清除 —— 这些是活代码。

**新增守卫**：`adapter-unit`「界面里没有账号相关的入口或文案」扫 `index.html` 与 `app.js`，
禁止那些文案/对话框 id/函数名回来，同时断言"本地档案不是账号"这句还在。

做法说明：这几块是连续大段（合计 500 行），我写了一个**一次性脚本按"起点行 + 终点行逐字匹配"**
来删（对不上就报错、不写文件），删完 `node --check` + 全套测试 + `git diff` 复核，脚本用完即删。

### ② 生成参数面板（温度 / top_p / 输出上限）

放在「设置 → 模型 → 生成参数（高级）」，默认折叠。三层优先级，**数字只在核心层定义一处**：

1. 渠道默认（本地 2048 / 官方 32768 的输出上限）
2. 用途档案（对话页 0.8 / 伴侣 0.85 / 剧情页 0.95）
3. 用户这一层：**跟随用途（默认）** / 稳一点（0.6 + 0.85）/ 活泼一点（1.1 + 0.95）/ 自己填

- 「自己填」的温度与 top_p **只在 manual 档生效** —— 这样旧版本存下的 0.8/0.9 不会把
  "跟随用途"悄悄钉死（这正是老配置与新默认打架的经典坑）；
- **输出上限**接通了：以前设置里存着 `max_tokens` 但聊天请求根本没用它（死配置）。
  现在填了就写进请求，也会同步到「发送前预估」那一行；填小了回复会被截断，
  而截断现在有明确提示 + 「接着说」（上一轮刚做的那套正好接上）。
  顺带解决了讨论里那个悬着的问题：**想让它说短点，用户自己填输出上限**，不用按模式硬压。
- 越界值会被夹到合法区间（温度 0–2、top_p 0.01–1、输出上限 ≥64），并在面板上写明当前生效的是哪一档。

**测试**：adapter-unit 91 → 97（预设/自己填/夹取/输出上限/请求体）；
local-app-check 59 → 60（切预设后**下一轮请求**的温度真的变了、输出上限写进请求且预估同步）。

### ③ `main` 合版与桌面安装包

- `codex/web-deploy` 快进合并进 `main`（两边同源，无冲突），`main` 现在是 0.1.16；
- 桌面安装包已在本机构建成功：`src-tauri/target/release/bundle/nsis/RoleWorld_0.1.16_x64-setup.exe`；
- **构建时抓到一个真问题**：`src-tauri/Cargo.toml` 的版本号从来没跟着升过（一直是 0.1.0），
  而 Windows「应用和功能」里显示的是 **Cargo.toml 的版本** —— 装出来的 0.1.15 会显示成 0.1.0。
  已把 Cargo.toml 纳入 `scripts/set-version.cjs`，并加守卫断言"四处版本号一致"
  （package.json / tauri.conf.json / Cargo.toml / app/version.json）；
- 另外：`tauri build` 默认会同时出 MSI，而 WiX 需要联网下载（这里超时了）——
  改用 `--bundles nsis` 只出 NSIS 安装包（release.yml 里记录的产物名就是 `RoleWorld_x.y.z_x64-setup.exe`）。

---

## 第四轮：修掉「记忆」栏显示示例数据（0.1.16 → 0.1.17）

用户问「这些功能怎么用呢」，我照着代码核入口时发现的真问题（**两个叠在一起**）：

1. **`index.html` 里有两个 `id="memoryList"`** —— 一个在角色记忆弹层里、一个在右侧「记忆」栏里。
   `querySelector` 只命中文档里第一个，于是集成层渲染的**真实记忆书被画进了弹层**，
   而右侧「记忆」栏永远是空的；
2. **`app.js` 里那份记忆书是写死的示例数据**（角色锁定书 / 场景记忆 / 精确事实 / 关系记录），
   而集成层 `applyMemoryPanelFilter()` 调的正是这个渲染函数 —— 即使没有 id 冲突，
   右栏显示的也是**假数据**。

修法：
- 右栏列表改名 `#memoryBookList`，渲染函数只认它（并写清为什么不能再用 `#memoryList`）；
- 删掉写死的示例书：渲染器改成**由集成层把真实记忆书传进来**（`renderMemoryBooks(visible)`），
  没传时渲染当前这一份、绝不编造内容；
- 新增两条守卫：`adapter-unit`「index.html 里没有重复的 id」；`local-app-check`
  「「记忆」栏显示的是真实记忆书」——拿**本机数据库里的真实条目内容**去对，
  同时断言示例书那几句写死的话一句都不许出现。

> 为什么不能按"书名"判断：示例书的名字（精确事实 / 场景记忆 / 关系记录）和内置包的
> 本地化书名是重的 —— 第一版断言就因此误报了一次，改成按内容判断。

**测试**：adapter-unit 97 → 98、local-app-check 60 → 61；九个套件合计 **313** 项。

---

## 第五轮：修掉顶栏「记忆」按钮点不到（0.1.17 → 0.1.18）

用户照着我写的用法说明去点，回了一句「**看不到记忆**」。核代码，是真的点不到：

`app/index.html` 顶栏那颗 `<button data-action="open-memories" … hidden>` 里的 `hidden`
是 P0 骨架从原来那份生产前端整段拷过来的，而**移植之后没有任何代码把它摘掉**——
`app.js` 只在开关面板时改它的 `aria-expanded`，从不碰 `hidden`；`styles.css` 里还有一条
`[hidden] { display: none !important; }` 把它按得死死的。
于是右侧「记忆」栏（0.1.14 才刚做完的那套）**唯一的用户入口是内部函数 `TASK21.openMemoryPanel()`**，
界面上根本点不到。无头 Chrome 探针实测：`hidden=true`、`display=none`、`0×0`。

**为什么九十个端到端用例一个都没抓到**：它们打开这个面板用的全是
`window.TASK21.openMemoryPanel()`；而 `element.click()` 对隐藏元素**照样会触发事件**，
所以"按钮能不能被用户点到"这件事从来没被验过。教训写进用例里了。

修法与守卫：

- 去掉那颗按钮的 `hidden`（并留注释说明为什么它不能再带上）；
- `adapter-unit` 新增静态守卫「主要入口按钮不能带 hidden」：按 `data-action` 抓按钮标签，
  带 `hidden` 直接判红，同时要求存在 `close-memories` 与 `open-memories` 的处理分支；
- `local-app-check` 新增「顶栏「记忆」按钮看得见、点得开」：先查几何
  （`hidden` / `computed display` / 尺寸 / 是否在视口内 / `elementFromPoint` 有没有被遮住），
  再用 **CDP `Input.dispatchMouseEvent` 发一次真鼠标点击**（不是 `element.click()`），
  点开后必须列出真实记忆书，最后用关闭按钮收回并断言右栏真的关上了。

顺带修掉一个发布脚本的漏项：`app/about.html` 正文和状态 JSON 里各写了一份版本号，
但它**不在 `scripts/set-version.cjs` 的同步范围内** —— 这次升 0.1.18 时它没跟着动，
全量测试当场变红（`关于页的状态 JSON …版本号与 package.json 一致`）。现已纳入同步并加写入后自检。

五种宽度实测（1440 / 1280 / 768 / 390 / 320）：按钮 48×32、都在视口内、顶栏无横向溢出；
`#topbarTitle` 的宽度在"有按钮 / 没按钮"两种状态下**一模一样**（123px / 窄屏 140px），
也就是说这次改回来没有挤到标题。

**测试**：adapter-unit 98 → 99、local-app-check 61 → 62；九个套件合计 **315** 项全绿。

---

## 第六轮：记忆面板里那句看不懂的英文（0.1.18 → 0.1.19）

上线后用户点开记忆栏，看到 4 本记忆书（精确事实 4 / 关系记录 1 / 角色锁定书 2 / 场景记忆 3），
第一条的标题是：

> Human confirmation edit: correction narrative removed so no superseded value appears in prompt context.

用户问「这内容对吗？」。查下来是**两件事叠在一起**：

1. **这 10 条不是用户聊出来的记忆**，而是内置「哈利·波特」角色包自带的记忆书
   （`packs/harry-potter/worlds/MB Harry — fact clips / relationship tracker / role lock /
   scene memories`）—— 从当年那份 SillyTavern 存档原样导过来的，里面「Lin（林）」是**那份存档里的
   玩家角色**（上海来的麻瓜出身学生）。**其中 3 条是 `constant: true`**，也就是**每条消息都会注入**
   （其余按关键词命中才注入）。用户如果换了个身份聊，哈利会一直以为对方是 Lin。
   → 这一条**属于产品/数据问题，需要用户拍板**（继续用 Lin / 删掉这些条目 / 包里默认不带）。
2. **那句英文是迁移痕迹当标题显示了**：SillyTavern 的 Memory Books 把"这条是什么"写在 `comment` 里，
   `integration.js` 的 `mapEntry` 直接拿它当标题（只剥掉 `[STMB] ` 前缀）。
   内置包里 `mb-fact-home` 的 comment 恰好就是转换器自己留的说明，于是它冒到了界面上。
   **它不进模型上下文** —— 提示词里只放 `[uid] content`（`task22-core.js` 的 `buildSystem`），
   所以只影响观感，不影响模型行为。

修法（第 2 条）：`memory-core.js` 新增 `entryTitle(entry)`，口径是
「有用的 comment → 正文开头 24 字（跟自动记忆自己写 comment 的口径一致）→ 关键词 → 条目 #uid」，
并把 `Human confirmation edit` / `correction narrative` / `automated conversion` 这类转换器说明判为噪音；
`integration.js` 的 `mapEntry` 改用它，并保留"核心模块不在时"的退化分支。

守卫：`memory-unit` 新增 4 条（正常 comment 保留、转换器说明被丢弃并退回正文、无 comment 用正文开头、
都没有时退回关键词/条目号）；`local-app-check` 的 fixture 里那条记忆的 comment **故意改成**
内置包里那句转换器说明，并在顶栏用例里断言面板不许出现 `Human confirmation`。

> 这条断言**先证明它抓得住**：把 `mapEntry` 改回旧写法跑一次，用例当场变红，报的正是用户看到的那句
> （`面板把迁移痕迹当标题显示了："…Human confirmation edit: correction narrative removed so no supe…"`）；
> 改回来再跑，全绿。

**测试**：memory-unit 38 → 42；九个套件合计 **319** 项全绿。

---

## 第七轮：把"别人的存档"从内置包里拿掉（0.1.19 → 0.1.20）

用户看完面板回了一句：「**我又不是 Lin**」。于是这一轮不是修显示，是修数据。

### ① 内置包里带的其实是别人那份存档

「哈利·波特」包里那 4 本记忆书来自**当年那份 SillyTavern 存档**：Lin（林）是**那份存档里的
玩家角色**（上海来的麻瓜出身学生）。其中 **3 条是 `constant: true`，也就是每条消息都注入**
（`MB Harry — relationship tracker` 的交情记录、`MB Harry — role lock` 里"Lin 的背景与到来"），
其余按关键词命中才注入。也就是说：任何人装上这个包、以自己或别的角色的身份跟哈利说话，
模型每一轮都会被提醒"玩家叫 Lin、上海来的"。

- 从包里**删除** 3 本整本都是 Lin 的记忆书：`fact clips` / `relationship tracker` / `scene memories`；
- `role lock` 里**删掉** `mb-role-shared-lin` 一条，**保留** `mb-role-timeline`
  （乌姆里奇上任 / 魔法部否认 —— 那是世界观设定，跟玩家是谁无关）；
- `packs/index.json` 的清单与版本号同步（version 2 → 3，描述改成"一本记忆书"）；
- 原件归档在 `runs/2026-09-12-lin-sample-memories-removed/`（含 README：为什么删、怎么装回去），
  没有不可逆损失。

### ② 已经装过旧包的存档不会自己变干净 —— 补一次性清理

内容包只在"这本世界书还不存在"时才安装（`adapter/packs.js`），升级不覆盖用户的世界书，
所以光是改包救不了老存档。新增 `app/adapter/pack-cleanup.js`：

- 冻结一张表：**条目 uid + 原正文里的一句特征串**，两者都对上才删；
- 用户改过内容的条目、以及表里没登记的书（哪怕正文里写着同样的话）**一律不碰**；
- 整本清空时才把那本记忆书删掉；
- 幂等：跑第二次什么都不会删；
- 启动时（`installAll` 开头）跑一次，写两个键：`packs:sample-cleanup-notice`（界面弹一次提示，
  读完清空）与 `packs:last-sample-cleanup`（不会被清掉的记录，出问题能查清了什么）。

界面上补一句话，否则用户只会看到书凭空少了：
`已清掉内置包自带的示例记忆 N 条（那是原存档里的玩家角色，不是你）`。

### ③ 顺带抓到的同类问题：整场端到端测试是在"被引导浮层盖住"的界面上跑的

写 ① 的用例时它先失败了，报的是「**「记忆」按钮被别的元素挡住：rw-ob**」——
`.rw-ob` 是首启引导浮层（`position:fixed; inset:0; z-index:99999`）。原来两个浏览器套件的 fixture
都没设 `tutorial_seen`，于是**主流程的几十条用例全都是在浮层盖着的情况下跑的**：它们用
`element.click()`，盖不盖都点得到，真人却一下都点不到 —— 跟上一轮「记忆」按钮 `hidden`
是同一类漏网。现在两个 fixture 都显式设置 `tutorial_seen: true`（量真正能用的那一层），
引导本身仍有一条专门用例：先清标记、刷新，再一步步走完。

### ④ 线上也清了一遍

云端静态托管只上传、不清理旧文件，那 3 份示例 JSON 发布会后仍能被下载 —— 已单独
`tcb hosting delete` 删掉，并在 `scripts/verify-live.cjs` 里加了一条"这几个文件线上必须 404"
的断言（现有文件返回 200、已删文件返回 404，两边都验过）。

**测试**：adapter-unit 99 → 101（清理的精准性 + 幂等）、local-app-check 62 → 63
（启动后示例条目消失、用户条目留下、整本示例的书被删、提示被消费）；
九个套件合计 **322** 项全绿。`verify-live.cjs`：48/48 逐字节一致、version.json = 0.1.20。

---

## 第八轮：让角色记得"之前发生了什么" + 原著剧情（0.1.20 → 0.1.21）

用户连着三句把需求说清楚了：「不应该弄全吗，之前的记忆」→「我是要让哈利等小说人物记得
之前发生了什么」→「我要的真实小说的剧情」。也就是两件事：**事件要记得住**，
而且记的该是**原著里真正发生过的剧情**，不是聊天里现编的东西。

### ① 事件记忆：新增 `[[事件: …]]`（机制）

原来只有一条通道：`[[记住: 主题 | 要点]]`，而且 `classifyMemory()` 会**确定性地拒收**带剧情特征的
内容（"他拔出魔杖""两人约好…"），提示词里也写着"不要记剧情"——所以角色跨对话记不住发生过的事。

- 新增标记 `[[事件: 一句话经过]]`：最多 2 条，与事实分开计数（事实仍是最多 3 条）；
- 存进同一个「自动记忆」书，但 `rw_source.kind = "event"`；
- **注入时一律带 `[此前发生]` 前缀**（`task22-core.buildSystemPrompt`），事实条目不带前缀 ——
  这是用户那条底线「真实信息和虚构剧情分开存」的落点：事件记得住，但绝不冒充"关于你的事实"；
- 事件**不参与"同主题替换"**：昨天去了图书馆、今天又去了，是两件事，合并等于抹掉历史；
- 上限满时挤占顺序：剧情 → **事件** → 其他（"你是谁"的事实最后走）；
- 面板里事件单独一类「事件（此前发生）」，每条也标出来；
- 设置里新增开关「记住发生过的事」（默认**开**，`auto_event_memory`）；关掉时既不教模型写事件，
  也不会把事件写进记忆。
- 刻意**不把主题词「剧情」当成事件**：剧情仍走记忆取向那道闸 —— 端到端用例里模型正是用
  `[[记住: 剧情 | *你挥动魔杖挡住了巨龙的攻击*]]` 试图把剧情塞进记忆的，那条用例现在仍然红得住。

### ② 原著剧情书：每个角色一本（内容）

内置包里原来只有一本世界观设定（role lock）。现在补了 6 本「原著剧情」，全部是**中文整理**、
不是原文摘录：哈利 12 条（身世 → 一~四年级 → 1995 年夏天 → 五年级开学 → 人物关系 → 能力弱点 →
塞德里克之死）、罗恩 10 条、赫敏 8 条、金妮 7 条、露娜 5 条、汤姆·里德尔 9 条。

**每本都停在角色卡固定的时间点之前**：哈利=五年级开学初，罗恩/赫敏/金妮/露娜=1994 年 11 月
（三强赛宣布后），汤姆=1940s 末~1950s 初。所以角色**不会预知未来** —— 每本都有一条
「他知道什么、不知道什么」写明知识截止点，这也和卡里原有的 lore 一致。

### ③ 守卫

- `adapter-unit` +6：`[[事件: …]]` 解析与正文剥离、`[[记住: 事件 | …]]` 也当事件、半截标记、
  事实 3 条 / 事件 2 条的上限、注入前缀 `[此前发生]`、开关关掉后不再教模型写事件；
- `memory-unit` +5：事件收下并带 kind、事件不参与同主题替换、两次同类事件各留一条、
  上限满时先挤事件、面板分组；
- `local-app-check` +2：端到端跑一轮"模型写事件 → 落库（kind=event）→ 标记不进界面 →
  下一轮请求里带 `[此前发生]` → 面板读得出"；以及 6 本原著剧情书按角色一一对应、
  每本都有知识截止点。

**测试**：adapter-unit 101 → 107、memory-unit 42 → 47、local-app-check 63 → 65；
九个套件合计 **335** 项全绿。`verify-live.cjs`：54/54 逐字节一致、version.json = 0.1.21。

---

## 第九轮：体验卡 —— 让不会配 Key 的同学也能用（0.1.21 → 0.1.22）

用户的原话：「能不能像发体验卡一样通过某种加密的方式提供这个 api key，我的同学有些不会用」。
先说清楚一件事：**把真 Key 加密塞进客户端是不行的** —— 应用最终必须拿明文 Key 去请求服务商
（放在鉴权头里），抓包就能看到；而且给出去就收不回来，只能整个换 Key、把所有朋友一起断掉。
所以"体验卡"必须是一张**能限额、能到期、能吊销的凭证**，真 Key 留在服务端。

### ① 中转服务 `relay/`（OpenAI 兼容，SSE 原样透传）

- `relay/server.js`：`POST /v1/chat/completions`（流式/非流式都透传）、`/v1/models`、`/card/quota`、
  `/healthz`，以及带口令的管理接口（发卡 / 列卡 / 停用 / 启用 / 吊销）；
- `relay/store.js`：账本三种后端（memory / file / cloudbase），入库的是 **sha256(卡号)**，
  不是卡号本身；文件后端"写临时文件再改名"，断电不会把整本账写坏；
- 卡的校验一律给人话：`CARD_UNKNOWN`（卡号抄错或已收回）、`CARD_DISABLED`、`CARD_EXPIRED`、
  `CARD_NO_CALLS`、`CARD_NO_TOKENS`，并说明上限是多少；
- 响应头回 `x-rw-card-calls-left / -tokens-left / -expires`，被拒时也给 —— 客户端能显示"剩 0 次"。

**两条硬规矩（写进代码 + 有测试守着）**：
1. **不记正文**：日志只有 时间/卡 id/模型/是否流式/状态/耗时/用量；
2. **账本只有哈希**：列卡接口也不返回卡号。

### ② 发卡工具 `scripts/card-cli.cjs`（本机跑，只跟管理接口说话）

`issue --label 小明 --calls 200 --days 30` 会打印**一段可以直接转发给同学的话**（卡号 + 三步用法 +
隐私说明）；另有 `list / disable / enable / revoke / quota / text`。
卡号只在发卡那一次返回（账本里只有哈希），发完就得复制走。

### ③ 应用侧：设置里多一行「体验卡」

- 同学粘**卡号**、**卡号@中转地址**、或**整条 `#card=…` 链接**都认（最后一种点开就自动配好）；
- 「使用体验卡」= 把接口指向中转 + 把卡号存进密钥位（`api_key_custom`）+ 查出剩余额度；
- 那一行下面写清了取舍：走中转时对方能看到「用了多少」，但中转服务端不记录聊天内容；
- 卡号存在 secrets 里，跟 API Key 同一条规矩：**不进导出存档**。

### ④ 测试

- 新增第 10 个套件 `tests/relay-check.cjs`（**20 项**）：起一个假上游 + 真的中转，覆盖
  卡的六种结局、用量记账、SSE 逐字节一致、CORS 预检与响应头暴露、模型白名单、上游报错透传、
  管理接口关闭，以及上面那两条硬规矩（日志里找不到用户那句话、账本里找不到卡号明文）；
- `adapter-unit` +3：三种粘贴形式（含整条链接的解码）、`looksLikeCard`、额度文案；
- `local-app-check` +1：用假中转把"粘贴 → 自动配置 → 显示剩 4 次"整条链路跑一遍。

**测试**：十个套件合计 **359** 项全绿。

### ⑤ 部署这一关卡在账号上（要用户出手）

`tcb cloudrun deploy roleworld-relay --source relay --port 8787` 的返回是：

```
× [CreateCloudRunServer] 云托管资源未开通
```

也就是这个 **体验版**环境没有开通云托管（容器）。要真正发卡给同学，二选一：
① 在 CloudBase 控制台**开通云托管**（体验版通常要先转成按量计费的付费环境）；
② 把中转放到别的常开主机上（自己的服务器 / 有公网 IP 的机器）。
**云函数不是好替代**：事件式调用没有流式，而且请求/响应体有大小上限，而本应用默认把整段历史
都发出去，长对话必然撞上限 —— 所以 `relay/scf.js` 只作为降级方案留着，并在文件头写明这一点。

---

## 第十轮：把体验卡真正接上云托管（0.1.22，2026-09-12 晚）

用户把云托管买下来了（体验版 → 个人版），于是这一轮全是"把服务跑通"的硬仗。
过程里每一步都是真报错、真定位，值得记下来：

| 症状 | 真正的原因 | 处理 |
|---|---|---|
| `× [CreateCloudRunServer] 云托管资源未开通` | 环境没开通云托管（不是等分配） | CLI 没这个命令，用 API 触发：`tcb api tcb CreateCloudBaseRunResource`，等 `ClusterStatus: succ`（分配了 VPC） |
| 部署卡在"是否灰度发布"/"平台有部署任务在跑" | CLI 的交互提示，管道空输入不生效 | 用 `"Y" \| tcb cloudrun deploy ...` 明确确认 |
| 上游 401 `Your api key: ****utBQ is invalid` | `UPSTREAM_KEY` 填的是云开发网关令牌，`UPSTREAM_BASE` 却还是 DeepSeek | 两边对齐（换 DeepSeek 的 `sk-…`，或把 base/path 改成网关） |
| 账本 `INVALID_ACCESS_TOKEN`（JWT `kid` 一直不变） | 容器里注入的是「AI 工具」页建的**网关令牌**，不是**环境级服务端 ApiKey** | 在「环境配置 → ApiKey 管理」新建 `relay-store`，再到云托管「API Key 设置」改选它 |
| 换成正确 ApiKey 后仍报 `Unable to find a signing key…` | `@cloudbase/node-sdk` 在容器里用 ApiKey 鉴权不通过（且官方已标注停止维护） | 弃用 SDK，改走官方 **HTTP API**（`Authorization: Bearer <ApiKey>`） |
| `DATABASE_COLLECTION_NOT_EXIST: … no document database instance … provisioned with PostgreSQL` | **这个环境是 PostgreSQL，不是文档型数据库** | 账本改走 **PG Data API**：建表 `POST /v1/rdb/exec-pgsql`（`role=cloudbase_postgres`），增删改查 `/v1/rdb/rest/{table}`（PostgREST，主键冲突 upsert） |

**最终形态**：`relay/store.js` 里三个云后端（`cloudbase-pg` 首选、`cloudbase-http` 文档库、
`cloudbase` SDK 兜底）；`CARD_STORE=cloudbase` 时按 PG → 文档库的顺序试，都不行才退到 file 并**大声告警**。
`/healthz` 回显真正在用的后端与注入 Key 的 `kid`；`/admin/store/selftest` 与 `/admin/upstream/selftest`
把"账本坏了"和"上游配错了"分开报（自检自己收尾，不留垃圾卡）。

**实测通过**（2026-09-12 21:40）：

```
healthz          → {"store":"cloudbase-pg","upstreamKeySet":true,"adminEnabled":true}
store/selftest   → {"kind":"cloudbase-pg","wrote":true,"readBack":true,"removed":true,"ok":true}
upstream/selftest→ {"ok":true,"status":200,"reply":"可用"}
发卡             → RW-5A87C-2903B-57F46（20 次 / 7 天）
真跑一轮         → HTTP 200、text/event-stream、首字节 264ms、50 个 SSE 帧
                   回复「嗨，今天在霍格沃茨过得怎么样？」
额度             → 0/20 → 1/20（剩余 19）✅
```

顺带修掉一个会坑人的地方：流式请求现在自动带 `stream_options.include_usage`
（否则上游不回 usage，卡的 token 额度永远是 0）；只在流式、上游是 DeepSeek 官方或显式开关打开时才加。

`relay-check.cjs` 从 20 → **28 项**（新增两个自检接口、PG 账本、文档库账本、include_usage）。

**边界照旧**：服务端只记用量（次数 / token / 时间），不记对话正文；账本存卡号哈希；
发卡口令轮换过一次（旧的曾出现在聊天里），新口令只在本机 `relay/admin-secret.local.txt`。

---

## 第十一轮：体验卡要"点开就能用"（0.1.22 → 0.1.23）

第一版发出去后用户立刻反馈两件事：**「别人打开还是必须填 apikey」**、
**「界面上有没有提示次数啥的，特殊提示」**。两条都成真了。

### ① 点开链接还被要求填 Key —— 三个原因叠在一起

1. **卡是在"该不该弹引导"之后才应用的**：`onboarding.maybeShow()` 先判断有没有 Key，
   发现没有就弹引导；等 `settings-ui` 里那个异步应用跑完，引导已经拦在前面了。
2. **`RoleWorld.init()` 在真实使用里根本没被调用**（只有测试显式调它）——
   我把自动应用挂在 `init()` 里，等于挂在一条永远不走的路径上；是测试自己调 `RoleWorld.init()`
   才让这条路径"看起来像存在"。
3. **只改片段地址不会重新加载页面**：同学在同一个标签页里粘链接时 `hashchange` 不重载，
   而当时没有任何监听。

修法：自动应用统一走 `RoleWorldCard.applyFromLocation()`（幂等），
在 `RoleWorld.init()`、`settings-ui` 的 `bind()`、`onboarding.maybeShow()` 三处都先跑一遍，
并加 `hashchange` 监听；`maybeShow()` 把"正在用体验卡"也算作"已经有凭据"，
所以**带卡进来不再弹首启引导**；引导里"必须已有 Key"的判断也接受体验卡。

### ② 次数要看得见（顶栏徽标）

- 顶栏模型徽标旁多了一颗**体验卡徽标**：`体验卡 · 剩 12 次`；≤3 次变黄（"只剩 N 次"）、
  用完变红（"次数已用完"）；点它直接跳到「设置 → 模型 → 体验卡」。
- 数据来源两处：启动时查一次 `/card/quota`；**每一轮回复的响应头**
  （中转回的 `x-rw-card-calls-left/-tokens-left/-expires`）—— 不额外请求，永远最新。
- 卡出问题时不吞错：402/401 的 `CARD_*` 错误显示中转的原话
  （"这张体验卡的次数用完了（上限 20 次）。……可以换一张卡，或填自己的 API Key"）。

### ③ 守卫

- `adapter-unit` +3：徽标文案/颜色档、从响应头刷新额度（含 `unlimited`/`never`，以及"没有这些头就别瞎猜"）、
  402/401 错误文案（普通错误不冒充体验卡问题）；
- `local-app-check` +1：**端到端复刻同学那一侧** —— 造出"第一次打开"的状态，带 `#card=` 链接整页加载，
  断言：卡自动配好、**不弹引导**、输入框可用、徽标显示"剩 4 次"、真发一句走中转拿到回复、
  徽标按响应头刷成"剩 3 次"。

**测试**：十个套件合计 **371** 项全绿（adapter 110 → 113、local-app 65 → 67）。
`verify-live.cjs`：55/55 逐字节一致、version.json = **0.1.23**。

---

## 第十二轮：全中文开关（0.1.23 → 0.1.24）

用户要求：「再进入网站的位置加一个是否开启全中文的选项吧，有些人看不懂英文」。
界面文案本来就是中文，真正的问题在**角色说什么语言**：内置角色卡是英文的
（描述、性格、开场白都写 English，卡里还标着 `language: "en"`），所以角色会用英文回话。

- **进门就能选**：首启引导第一步（欢迎页）加了一个默认勾上的复选框
  「全中文：角色一律说简体中文（推荐）」；勾一下立刻生效（不等走完引导）。
- **设置里也能改**：「设置 → 模型 → 全中文」，即时开关。
- **实现**：`task22-core.languageLine(card, options)` 在 `options.fullChinese === true` 时
  **压过角色卡自己的语言设置**，给出明确要求（台词 / 旁白 / 动作描写都用中文、别照抄英文设定、
  之前用英文说过的先用中文重说一遍）。语言要求仍然**两头都放**（开头一处 + 格式指令之后再一处）。
- **剧情模式页**也跟着这个开关走（`magic-map.js` 传 `fullChinese`）。
- **默认值**：`full_chinese: true`（面向中文使用者；老调用方不传这个选项时行为完全不变）。

### 途中修掉的两个连锁问题（都属于"测试没覆盖到"那类）

1. **选项根本没进提示词**：`buildGeneratePayload` 组装时只把 `autoMemory/extraSystem/purpose`
   转发给 `composeMessages`，新参数被丢掉了 —— 端到端用例当场抓到（请求里没有那句中文要求）。
   顺带把 `autoEventMemory` 一起补上（此前是靠"默认 true"侥幸生效）。
2. **「本次请求」面板与真实请求对不上**：面板是"按分段重建系统提示再逐字节比对"，
   而全中文那行在真实提示里出现**三次**（开头、中间、结尾），面板只重建了中间那一次，
   于是它诚实地报了"分段统计与真实请求没有完全对上，这份数字不可信"。
   修法：面板也补上开头与结尾两处（与 `buildSystemPromptWithFormat` 的拼法一致）。

**测试**：`adapter-unit` +1（全中文压过卡设置、关掉回到卡的语言、不传选项保持老行为、
语言要求出现两次且结尾那处在格式指令之后）；`local-app-check` 里那条"全中文"用例
**真的发两句**（默认开着时请求里必须有中文要求；在设置里关掉后下一句就不能再有），
并在首启引导用例里断言"进门能看到这个选项、默认勾上"。
十个套件合计 **373** 项全绿；`verify-live.cjs`：55/55 逐字节一致、version.json = **0.1.24**。

---

## 第十三轮：用户说"还是英文" —— 两件事一起做（0.1.24 → 0.1.25）

上线全中文后用户截了聊天记录回来：哈利仍然整段英文。这一轮做了两件事：
**把要求写得压得住**，以及**给"浏览器还在跑旧代码"这种可能留一个能自查的出口**。

### ① 语言要求改成中英双语硬要求

原来只有中文那一句。现在 `languageLine(card, {fullChinese:true})` 是：

> 一律用简体中文回复：台词、旁白、动作描写、称呼都用中文，不要夹英文句子。角色卡里的英文设定
> 你自己看懂就行，不要照抄成英文原文。**即使之前的对话、开场白、角色卡都是英文，也从这一句开始
> 换成中文；先用中文把上一条的意思重说一遍，再继续。** (Hard requirement: reply in Simplified
> Chinese only — dialogue, narration and actions included — regardless of the character card language
> or the language of earlier messages.)

两种语言各写一遍是刻意的：整段历史都是英文时，只给中文要求有时压不住。这一行仍然出现在**两个位置**
（中间 + 格式指令之后）。

### ② 「运行中的版本」—— 先排除"浏览器还在跑旧 JS"

网页版有离线壳（service worker），只比对线上 `version.json` 看不出**浏览器里跑的是哪一版** ——
于是会出现"我明明发了新版，用户那边还是旧行为"，而且没法自查。所以：

- `scripts/set-version.cjs` 现在多写一个 `app/build.js`（`window.ROLEWORLD_BUILD = "x.y.z"`），
  版本号直接编进前端代码；`adapter-unit` 的"版本一致"守卫从四方扩到**五方**。
- 「设置 → 关于」多一行**版本**：`运行 0.1.25 · 已是最新`；与线上不一致时变红并提示
  「点右边『检查更新』刷新页面」，旁边那颗按钮直接走 `ROLEWORLD_PWA.reloadForUpdate()`
  （让等待中的离线壳接管再刷新）。

### ③ 顺手修掉一条真 flake

`local-app-check` 的"生成参数面板"用例偶发失败（改了输出上限 600，请求里却还是 32768）：
它只等"设置已落盘"，而页面的采样参数来自**内存里**的设置，异步重读可能还没回来。
现在多等一个信号：「发送前预估」那一行显示出 600 再发。全量套件连跑两次都是绿的。

**测试**：十个套件合计 **374** 项全绿（local-app 68 → 69）；
`verify-live.cjs`：**56/56** 逐字节一致、version.json = **0.1.25**。

---

## 第十四轮：再压一次"还是英文"，然后把交接写了（0.1.25 → 0.1.26）

用户第二次反馈同一件事。前两处语言要求都在**系统提示**里（最前面），而那段对话整段都是英文 ——
模型会跟着历史走。所以这一轮加**第三处**：全中文时在**用户这句话之前**插一条独立的 system 消息：

```
[Language] 必须用简体中文回复：台词、旁白、动作描写都用中文，不要用英文。
Reply ONLY in Simplified Chinese, including narration and actions.
```

- 刻意不塞进 user 消息（那会污染"用户原话"，记忆来源与改口解析都靠它）；
- 关掉全中文时一个字都不多带；
- 常量抽成 `FULL_CHINESE_REMINDER`，`describeRequest` 用同一份文本重建 ——
  否则面板的"分段之和 == 真实请求"核对会当场变红（这次就是它抓到的：7 条消息 vs 6 条）。

**测试**：`adapter-unit` 断言这条 system 紧贴在最后一条 user 之前、关掉时不存在；
`local-app-check` 的假模型端点新增 `tailMessages` 记录，端到端断言顺序。
十个套件 **375** 项全绿；`verify-live.cjs` 56/56，version.json = **0.1.26**。

**另外**：这一轮结束时按用户要求写了交接文档 `docs/HANDOFF_2026-09-12.md`
（现状 / 代码地图 / 常用命令 / 凭据边界 / 12 条已知坑 / 待拍板事项 / 用户偏好 / 接手自检）。

---

## 第十五轮：默认回到"角色自己的语言"，并给每个角色一个语言开关（0.1.26 → 0.1.27）

用户两句话定的事：

> 「应该是可以开启强制全部中文，默认应该是角色自身的语言」
> 「或者说要不每个角色都弄一个语言开关选项？」

前几轮一路往"一律中文"加码（0.1.24 开关默认开 → 0.1.25 中英双语硬要求 → 0.1.26 贴近本轮输入的提醒），
这一轮把**默认值反过来**：不强制，按角色卡自己写的语言说话；强制变成"想开才开"的选项，
而且**可以按角色分别设**。

**实现（两级设置）**：

- 全局「设置 → 模型 → 角色语言」：`language_mode` = `auto`（默认，跟角色卡）｜`zh`｜`en`。
- 单个角色：`language_by_card[avatar]` = `zh` / `en`，**没有这一项 = 跟随全局**。
  入口两个：对话页顶栏新增的「语言」下拉（与模型徽标同一排），以及「设置 → 角色管理」每一行里的
  同名下拉 —— 两处写的是同一份设置，改哪个另一个都会跟着变。
- 优先级：单角色 > 全局 > 角色卡自己的语言（`task22-core.languageFromSettings`，纯逻辑、可直接单测）。
- 旧的布尔 `full_chinese` **退役**：不再参与判断，读设置时顺手删掉。它当年的默认值是 `true`
  （等于"没选过也是中文"），留着只会和"默认跟角色卡"打架。
- 「贴近本轮输入」那条提醒改成一式两份（中/英），并且**只在玩家自己选了语言时**才插：
  按角色卡说话时没有要压的东西，一个字都不多带。
- 伴侣模式段落也跟着走（`companionLangFor`）：强制中文/英文时用那个语言，没强制才看卡片语言 ——
  否则"一律中文"的会话里会夹一段英文规则。
- 首启引导的「全中文」勾选**默认不勾**，勾上写 `language_mode: "zh"`（进门那句"看不懂英文就勾上"保留）。

**顺手修的**：`about.html` 套件清单里 `relay-check` 还写着 20（实际 28，0.1.23 起就是这个数了）。

**测试**：`adapter-unit` 那条"全中文"用例改写成"角色语言"（默认跟角色卡 / 一律中英各自成立 /
两级设置谁压谁 / 老调用点 `fullChinese` 仍等价于"一律中文"）；`local-app-check` 换成端到端五步：
默认不强制 → 顶栏把这个角色改成中文（下一轮生效、只记这一个角色）→ 改回「跟随设置」→
全局「一律简体中文」生效 → 「角色管理」每行都有开关，且能给单个角色改成英文压过全局。
十个套件 **374** 项全绿（数量没变：两处都是改写而非新增）。

> 计数更正：0.1.26 那条记的"375"是加错了，实跑是 374（114+47+21+15+27+69+35+11+7+28）。

**已上线 0.1.27**（用户说"上线"后执行）：`tcb hosting deploy app -e cyan1-d2gpky2z903b86182 --verify --safe`
（56 个文件），`npm run verify:live` → **56/56 逐字节一致**、线上 `version.json = 0.1.27`；
再实测线上：`index.html` 有新的顶栏「语言」下拉与「设置 → 模型 → 角色语言」，
旧 `data-roleworld="full-chinese"` 已经不在，`task22-core.js` 带 `languageFromSettings`。
提交 `6d9cbf9`（本地，没推远端）。

---

## 第十六轮：「为什么我登的时候还是要输 apikey」（0.1.27 → 0.1.28）

用户连着两句：先说"这个卡是不是在一个地方登之后，另一个地方就登不了了"，再问
"为什么我登的时候还是要输 apikey"。查下来**卡本身没有任何设备绑定**（`relay/server.js`
里只有"卡在不在 / 停用 / 到期 / 用完"四个判断，整份 relay 搜不到 device、session、lock），
所以现象只能来自一件事：**这一次打开的地址里没有带那张卡**。而当时的两个缺口正好把人卡死：

1. **发卡文案教错了**。`scripts/card-cli.cjs` 的 `shareText()` 手动兜底那一步写的是
   "粘贴卡号 → 点使用体验卡"。可是新设备 / 换浏览器时本机并不知道中转地址，
   `card.apply()` 必然报"这条信息里没有中转地址" —— 同学照着发卡人给的话做，**必然失败**。
   改成直接给**可以整行粘贴**的 `卡号@中转地址`（新增 `pasteLine()`），并写明
   "只粘卡号是不够的：手机和电脑互不相通，新设备必须知道中转地址"。
2. **首启引导只有 API Key 一条路**。链接被聊天工具截掉、或者同学自己打开首页（地址里没有
   `card=…`）时，第二步只摆着 API Key 输入框 —— 手里有卡的人也会以为"我必须去要个 Key"。
   现在这一步多了「用体验卡」：粘卡号那一整行 → `RoleWorldCard.apply()` → 卡号进密钥位，
   `keyReady()` 认它，直接往下走。**而且不点那个按钮、直接点「下一步」也会顺手用掉**
   （同学的直觉就是"粘上、点下一步"）。

顺带把话说清：`apply()` 缺中转地址时的报错现在给出能直接粘贴的形式；
「设置 → 模型 → 体验卡」那一行的 placeholder 与说明也改成"新设备要粘一整行"。

**测试**：`adapter-unit` +1（没有中转地址时挡住并说清缺什么 + 静态钉住发卡文案必须给
`pasteLine`、必须写"只粘卡号是不够的"）；`local-app-check` +1（**地址里不带卡**整页打开 →
引导第二步出现「用体验卡」且真的能看见 → 粘 `卡号@中转地址` 并**故意不点按钮**直接点下一步 →
服务商/接口地址/密钥位都对、顶栏"剩 4 次"、输入框可用）。十个套件 **376** 项全绿。

> 给用户的话：**换设备/换浏览器时，"卡号@中转地址"那一整行才是能用的东西**；
> 已发出的那 5 张卡可以用 `npm run cards -- text <卡号>` 重新生成这段话发一遍。

---

## 第十七轮：给"用卡进来的人"一个正式的（0.1.28 → 0.1.29）

用户：「就是我觉得通过卡进入的应该也要有个正式的开始的步骤」。说得对 ——
0.1.23 为了避免"打开还要填 API Key"，把带卡进来的人**直接从引导里摘了出去**，
结果是他们被扔进对话：不知道这是什么卡、还剩几次、到期没到期、角色为什么说英文、
记录存在哪、用完了找谁。

所以这一轮给卡用户补了**另一套开场**（不是原来那套"填 Key"的引导）：

| 步骤 | 说什么 |
|---|---|
| 你拿到了一张体验卡 | 不用填 Key；额度（`剩 4 次 · 到期 …`，来自中转 `/card/quota`）；用完了找发卡的人；**聊天记录只存在这台设备上**，中转只统计次数 |
| 怎么称呼你 | 复用原来那一步（`preferences.nickname`），以后角色就这么叫他 |
| 角色说什么语言？ | 内置卡是英文的 → 默认说英文；看不懂就勾「让角色一律说简体中文」（写 `language_mode`） |
| 可以开始了 | 左侧选角色、每个角色有自己的记忆、再报一次额度、想用自己的 Key 去哪里填 |

**判重靠新键 `card_welcome_seen`**，不能复用 `tutorial_seen`：老同学第一次打开链接时
（老版本为了不弹"填 Key"那套）`tutorial_seen` 就已经被置成 true 了，用它会永远不弹。

顺带修掉一个一直没人注意的显示问题：`onboarding.js` 里的 `format()`（把 `**强调**` 转成
`<strong>`）**从来没被调用过** —— 引导文案里那些 `**称呼**`、`` `%APPDATA%…` `` 一直是**字面**显示的。
现在渲染前统一走 `renderMarkdown()`（只替换文本标记，不动我们自己的标签）。

**测试**：`local-app-check` 里那条"同学点体验卡链接"改写成完整开场用例：
把"老同学的浏览器"造出来（`tutorial_seen: true`）→ 带 `#card=` 链接整页打开 →
断言弹的是**体验卡开场**、文案里有"不用填 API Key"和"剩 4 次"、**没有 API Key 输入框** →
走完四步（称呼 / 勾「一律中文」）→ 断言 `card_welcome_seen`、`language_mode`、称呼都落盘 →
重载不再弹 → 顶栏"剩 4 次" → 真发一句走中转、徽标刷成 3。
十个套件 **376** 项全绿（这条是改写，数量不变）。

---

## P0-1 多视口布局回归 ✅

**新增** `tests/viewport-check.cjs`（25 项，已加入 `npm test`）。
自带静态服务与假模型端点，覆盖 1440×900 / 1280×800 / 768×1024 / 390×844 / 320×568。

**它当场抓到两个真实缺陷**（都已修）：

1. **平板 768px 下发送键被压成 23px 宽** —— `.send-button` 的 44px 规则只在 760px 以下生效，
   而 `.composer-footer` 是 flex，按钮被压缩。修法：基础规则加 `flex: 0 0 auto`。
2. **「记错 / 编造」按钮命中区只有 22×14** —— 鼠标都难点、触屏更点不到。
   修法：基础提到 26×24，窄屏（≤760px）提到 44×44。
   窄屏判据用**宽度**而不是只看 `(hover: none)`：实测无头 Chrome 把 hover 报成 hover，
   只按媒体查询会让手机拿到小按钮。

**顺带修掉一个我自己造成的事故**：之前用 PowerShell 追加中文 CSS 注释时被写成 **GBK**，
`app/styles.css` 混进 **153 处非法 UTF-8 字节**（浏览器把中文注释全变乱码、编辑器打不开该文件）。
已按 GBK 解码后重写为 UTF-8。

**并加了一条永久防线**：`adapter-unit` 新增「仓库文本文件是合法 UTF-8」用例，
逐字节校验 `app/`、`tests/`、`docs/` 下所有文本文件。已用"故意植入坏字节"验证它真的会报错
（既有的 BOM 检查抓不到这种编码错误）。

**提交**：`fd275aa`（修复）、`72c9961`（清理误提交的临时文件）
**测试**：adapter-unit 69/69、memory-unit 25/25、search-unit 15/15、metrics-unit 13/13、
inventory-probe 14/14、local-app-check 39/39、viewport-check 25/25

---

## P0-2 / P0-3 / P0-4 数据完整性 · 压力 · 隐私出口 ✅

**新增** `tests/data-integrity.cjs`（10 项，已加入 `npm test`），自带静态服务与假模型端点。

### P0-2 存档往返

- 导出再导入后，记忆的**来源元数据一字不差**（主题 / 替换记录 / 来源对话 / 消息序号 / 时间戳 / 来源标记）；
  对话正文与角色卡名称同样保留。
- **抓到一个真隐私问题并修掉**：`exportAll()` 会把 `kv` 里的 `secrets` 一起导出 ——
  也就是**存档 zip 里带着明文 API Key**。存档是用户会随手发出去的东西，这等于把凭据送人。
  修法：导出时跳过密钥键，并在导出提示里写明"存档里不含 API Key，换机器后需要重新填一次"。

### P0-3 长对话压力（800 轮 / 1601 行 / 198 KB）

实测：写入 **5ms**、列表 **1ms**、读取 **2ms**；1600 条消息里检索 **9ms** 且命中的是目标那一轮；
界面上仍能打开、能发送、台账照常记一轮。**没有发现性能问题。**

### P0-4 隐私出口

- 请求体里**没有 API Key**；
- 甲角色的请求里**没有乙角色的对话与记忆**（角色隔离在出口处也成立）；
- 请求体里没有本机偏好字段（preferences / 教学状态 / 侧栏选择 / 称呼）；
- 请求体是干净的 OpenAI 形状：SillyTavern 时代的内部字段被适配层剥掉了；
- 全部请求只发往一个路径。

**提交**：`12db082`（含隐私修复）、`d0e4c60` 之后的清理提交
**测试**：全套 8 个套件全绿（69 / 25 / 15 / 13 / 14 / 39 / 25 / 10）

---

## P1-1 / P1-2 / P1-3 记忆整理 ✅

**提交**：`3ff5409`

- 记忆面板按主题分组、可折叠、显示"已用 N / 上限 M"；
- 可调上限（设置 → 模型 → 记忆条数，5~500，默认 50），超出时明说挤掉了哪几条；
- 一键清空该角色全部记忆（二次确认），清完下一轮请求里**一条不剩**（有用例断言）；
- 来源可追溯：每条记忆都能点「看原话」跳回来源那句；来源对话若已不在，给出说明而不是报错。

---

## 追加：AI 写角色改成两步 ✅

**提交**：`ba4a63b` · 用户当面的要求：「应该是用户描述角色再让 ai 生成适当长度的提示词再丢给 ai 生成角色卡片」

- 第一步「生成提示词」把一句话描述扩写成设定稿（名称 / 身份与处境 / 性格 / 说话方式 /
  关系 / 边界 / 开场情境），目标 900 字、400~2000 字区间；偏短偏长都当场提示，**不丢弃**模型给的内容；
- 第二步用**用户改过的那份稿子**写卡（用例断言：写卡输入里带着用户补的那句）；
- 「跳过，直接生成」保留原路径；
- 新增这次创建的总调用预算 5 次（写卡本身仍最多 2 次），避免一路重试把额度烧光；
- 实测：扩写 155 字 → 写卡输入 169 字（含用户补写）。

---

## 追加：伴侣模式（P5-1 / P5-2）✅

**提交**：`6b720d0` · 方案与后续建议见 `docs/COMPANION_MODE_PLAN_2026-09-11.md`

- 新增 `app/companion-core.js`：关系档案（关系 / 称呼 / 关系起点 / 共同经历）、
  时间感（今天几号、上次聊天几天前）、6 条硬规矩、内疚话术自检；
- 存 `kv` 的 `companion:<角色>`，**不进记忆书** —— 你写的真实信息和模型记的东西分开存，
  「清空记忆」不会碰它；存档往返一字不差（有用例）；
- 段落大小实测：固定部分 **332 字**（约 200 token），满配（8 件共同经历）**775 字**；关掉 = 一个字符都不多发；
- 界面：角色记忆 →「关系档案」，实时显示"每轮会多带约 N 字（约 M token）"；
- 英文卡拿英文段落（中文段落会把英文角色带跑）；
- 自检只报告内疚话术、**不改写**模型的话（过滤会误伤，拦住了也不等于没发生）。

---

## 追加：「上次说到」+ 修掉一个真缺陷（旧对话打不开）✅

**提交**：见本文件末尾的提交列表

「上次说到」：隔了一天以上再打开一段对话，输入框上方写一行
`上次说到：<你上次说的那句原话>（上次聊天是 N 天前）`，可以「以此开头」填进输入框。
纯本地计算，**不调模型、不做摘要、不编内容**；没有记录就不显示。

**顺带抓到一个真缺陷（这次最值得说的）**：会话表里的对话名按约定不带 `.jsonl`，
而**老数据 / 导入进来的对话存储键是带 `.jsonl` 的**。打开这类对话时按去掉后缀的名字去查，
查不到 → 界面提示"这段对话已不可用"。也就是说：**导入过的旧对话其实打不开**。

- 之前那条「看原话能跳回来源消息」的用例是**假通过**：它等的"团子"其实来自上一轮对话里
  残留的回复，跳转本身失败了也没被发现；
- 修法：会话上多记一个 `storageFileName`（真正的存储键），加载时两种后缀都试一遍，
  试到哪个就用哪个，保存 / 归档 / 删除一律用同一个键 ——
  否则会出现"打得开、但新消息另存成第二份"这种更糟的情况；
- 新增两条用例：旧对话能打开并显示原话；接着聊之后**记录数不变**、
  新消息确实写回原来那份（防的正是"另存一份"）；
- 同时修了检索里"跳过当前对话"的比较（以前比的是去掉后缀的名字，等于没跳过，
  当前这段对话会被当成"历史"再塞回给模型一遍）。

---

## P2-1 发送前预估 ✅ / P2-2 输出上限与上下文分开 ✅

**提交**：见本文件末尾的提交列表

**P2-1 发送前预估**：输入框上方多了一行
`这次约 12.3k 输入 ≈ ¥0.0123（命中缓存 ≈ ¥0.0021） · 输出上限 32.8k`，悬停给出完整口径。

- 估法：**上一轮的真实用量**（接口回 usage 就用真值）+ 它上一句回复（下一轮会进历史）
  + 你输入框里现在的草稿。**不额外发请求、不重拼一遍提示词**（拼出来只会是另一个近似值）；
- 输入框里打字会实时更新（250ms 防抖），字越多预估越高；
- 接口没回 usage 时按字数估，并在悬停里明说"只能看量级"，不假装精确。

**P2-2 上下文与输出上限分开**：

- `task22-core` 新增 `contextLimitFor` / `outputLimitFor` / `checkContextBudget`：
  上下文是模型硬上限（本地 32k、DeepSeek 1M），输出上限是请求里写的 `max_tokens`（2048 / 32768）；
- 「本次请求」面板多一行：`上下文：输入 N token（占 1M 的 3%） + 输出上限 32768 = …，在模型上限之内`；
- **发送前检查**：输入 + 输出上限超过上下文时，**直接不发**，并在对话页写明该改什么
  （调小历史预算 / 减少记忆条数 / 换模型 / 新开一段对话），而不是等接口回 400 再让用户猜。

**顺带修了测试端的一个盲点**：假模型端点从来不回 usage，于是"按真实用量估算"这条路径
在浏览器用例里**从来没被跑过**。现在假端点会像真实接口一样在最后回一段用量（含缓存命中数），
台账与预估都按真实值走。

**测试**：adapter-unit 76 → 80（上下文/输出的取值、超限提示、脏数据）；local-app-check 54 → 55。

---

## P2-3 检索结果引用 ✅

**提交**：见本文件末尾的提交列表

回复下面会标一行「引用了 N 条以前的记录」，点开能看到**原话**（带出处与时间），
再点一下跳到那段对话并高亮这一句。

- 判定很保守：回复和那条记录之间必须有**一段逐字相同的原话**（至少 5 个字连续相同）才算。
  找不到就不显示 —— 宁可少标，也不让"它大概是在说这条"这种猜测出现在界面上；
- 允许改代词：模型把「我养了一只猫」说成「你养了一只猫」仍然算引用（按最长共同片段判，
  不是整句包含）；但只凭几个常用字撞车不算；
- 标注写进消息的 `extra` 里，**刷新之后仍然看得到出处**；
- 没用到的轮次不显示这个入口（有用例专门盯着"没引用也挂入口"）。

**测试**：search-unit 15 → 21；local-app-check 55 → 56。

---

## P3-2 确认无主动提醒机制 ✅ / P3-3 状态页 ✅ / P4-1 故障排查 ✅ / P4-2 验收清单 ✅ / P4-3 数据与隐私说明 ✅

**提交**：见本文件末尾的提交列表

- **P3-2**：写成用例而不是口头保证 —— `adapter-unit` 直接扫 `app/` 下的代码：
  不许出现系统通知 / 推送 / 后台同步 API，不许有 ≥5 分钟的定时器（界面防抖不算），
  界面文案里不许出现打卡、签到、好感度、召回那类词，内疚话术只允许出现在
  "禁止它们"的 `companion-core.js` 里。**它当场抓到了两个真问题**（我自己的说明页里
  写了"连续打卡/好感度"，以及一个 10 秒的下载 URL 回收定时器被误判），阈值和范围都据此调准。
- **P3-3 + P4-3**：`app/about.html` 扩成状态页：版本号、8 个套件的项数与覆盖范围、
  会/不会离开设备的内容、已知限制，另附一段机器可读的 `roleworld-status` JSON
  （页面直接渲染它，`adapter-unit` 会校验它能解析、版本号与 `package.json` 一致）。
  **顺手改掉一处不准确的说法**：原文写"API Key 不会上传到任何服务器"，
  实际上它会作为鉴权头发给你配置的那个端点 —— 现在写清了，用例也盯着这句话不许回来。
  README 增加「数据与隐私：本地保存 ≠ 不上云」一节。
- **P4-1**：`docs/TROUBLESHOOTING.md`（免费版提示页、Key 无效/限流/余额怎么分辨、
  费用口径、记错与改口怎么办、数据在哪与怎么备份、怎么回退版本）。
- **P4-2**：`docs/ACCEPTANCE.md` —— 你那张阶段表逐条落成可勾选清单，
  每条写清状态（✅ / 🟡 / ⛔）、依据（哪条用例）、以及**你自己怎么验**。

---

## P3-1 PWA（可装到桌面/手机 + 断网也能打开）✅

**提交**：见本文件末尾的提交列表

先纠正我自己的判断：上一轮我把它记成"需要你拍板、先不做"，但它本来就在你的清单里，
而且验收标准你写得很清楚（断网仍能打开、有新版提示刷新）。这属于**该做的活**，不是要你决定的事，
所以这一轮补上了。

- `app/manifest.webmanifest` + `app/icons/`（192/512，由一个命令生成，跟桌面端图标同一份画法）；
- `app/sw.js` 离线壳：**网络优先**，缓存只做兜底。为什么不用缓存优先：缓存优先最容易出的问题是
  "线上改了、用户那里还是旧的，刷新也没用" —— 那比"离线打不开"更难查。取舍写在 `sw.js` 注释里；
- `app/version.json` + `app/pwa.js`：加载时记下线上版本，**切回标签页时**再对一次，
  不一致就弹一个小胶囊「有新版本（x.y.z），点这里刷新」。刻意不做定时轮询
  （既有守卫用例禁止 ≥5 分钟的定时器，而且用户没看页面时提示也没意义）；
- 版本号三处同步：`node scripts/set-version.cjs` 现在同时改 `package.json`、
  `src-tauri/tauri.conf.json` 与 `app/version.json`，并有用例盯着三者一致。

**新增第 9 个套件 `tests/offline-check.cjs`（7 项）**，在真实浏览器 + 真断网（关掉服务器）下验证：

1. 清单能解析、图标真的取得到、`index.html` 真的挂了清单；
2. 离线壳注册成功并**接管页面**（`navigator.serviceWorker.controller`）；
3. 线上没发新版时不打扰；发了新版（`version.json` 变了）时提示里带出新版本号；
4. 切回标签页时自己重查一次版本（不用定时器）；
5. **关掉服务器再刷新：界面照样打开**，角色卡与以前那段对话都读得到、点开能看到原话，
   离线发送失败时把输入内容还给我、不会卡在"正在生成"；
6. **平台提示页不会被当成应用缓存下来**（见下）。

### 写这条用例时抓到的两个真问题

1. **`response.clone()` 用错了时机**：我原本在异步回调里才 clone 响应去写缓存，
   那时响应体已经被页面读掉，`clone()` 直接抛错 —— 结果是**导航页其实一直没被缓存**，
   只有安装时的预缓存那几份文件能用（所以离线"能开"掩盖了这个 bug）。
   改成**同步**克隆两份（一份判内容、一份写缓存）之后才是真的按设计走。
2. **平台提示页会毒化缓存**：托管平台首次访问会插一个"免费版/额度"提示页（你实际遇到过），
   它同样是 200 + HTML。如果被当成外壳缓存，**以后断网打开看到的就是那个提示页**。
   现在导航响应先确认"这确实是我们的应用"（页面里有「角色世界」或 `task22-core.js`）才缓存；
   并且我**故意把守卫去掉跑了一遍**，确认这条用例真的会红（不是一条永远绿的假用例）。

顺带把 P3-2 那条守卫用例改准了：它原本一见到 `serviceWorker.register` 就报，
而离线壳并不会主动联系用户 —— 真正的判据是"壳里不许有 push / notification / periodicSync 处理"，
以及全仓库不许有通知与推送 API。改完照样能抓到（我加了一行 `addEventListener('push')` 验证它会红）。

---

## 收尾：布局回归与文档里的数字同步

- `viewport-check` 30 → 35：五种宽度下检查"发送前预估那一行不与费用行/发送键重叠、
  窄屏自动让位、发送键不被挤小"（这类"控件互相压住"是纯视觉问题，只能用几何断言盯）；
- `app/about.html` 的状态 JSON 与 `docs/ACCEPTANCE.md` 里的套件项数更新为最终值
  （当前：97 / 38 / 21 / 15 / 27 / 60 / 35 / 11 / 7，九个套件合计 **311** 项检查）；
- 关于页的版本号由 `adapter-unit` 盯着，和 `package.json` 不一致就直接报错。

### 补记：上下文预检的"本地端点"口径，以及我自己踩的一个坑

P2-2 的预检需要知道上下文上限。本地/自定义端点各家不同（8k / 32k / 128k 都有），
拿一个数字硬猜会误拦，所以加了设置项「本地端点上下文（token）」，默认 32768。

**加完立刻踩坑**：这个默认值被当成了"所有模式"的上限，套到 DeepSeek 官方（1M）头上，
于是"输入 + 输出上限 32768"必然超过 32768 —— **每一次发送都被自己的预检拦下**。
`local-app-check` 在几分钟内就把它抓出来了（20 多条用例同时变红，全部指向"根本没发出请求"）。

修法：这个设置只对本地/自定义端点生效（`contextBudgetOptions` 里先判断是不是已知云端模式），
并补两条守卫：`adapter-unit` 断言"官方 1M 不该被拦"，`local-app-check` 断言面板上写的上下文是
`1000000` 而不是设置里的 32768。**这类"设置悄悄改变了主流程"的问题，只有端到端用例拦得住。**


---

## 待你决定

1. **伴侣模式接着做什么**：`docs/COMPANION_MODE_PLAN_2026-09-11.md` 第 5 节列了三个问题 ——
   ① 伴侣模式下回复要不要默认变短（我建议 300~500 token，但这是体验偏好，得你点头）；
   ② 预算到顶是只提示还是直接拦；③ 主动消息做不做（我建议先不做：那是唯一有可能伤到人的部分，
   而且网页端根本做不到"应用没开也会发"）。
   你没回的话，我会按方案里写的先做 P5-3（记忆取向）和 P5-5（月度台账/预算）——
   这两项不改变现有体验，也不需要你拍板。
2. **今晚这些改动要不要发上线**：我没有部署、没有推 `main`、没碰任何凭据，全部只在
   `codex/web-deploy` 分支上。PWA 的离线壳会在用户浏览器里注册一个 service worker，
   所以这一项**上线前最好你先在本机点一遍**（安装图标、断网刷新）。
3. **桌面端安装包**：仓库版本号已经是 `0.1.14`，Release 上还是 `0.1.13`。要不要出包由你决定。

---

## 今晚的提交（都在 `codex/web-deploy` 分支上）

| 提交 | 内容 |
|---|---|
| （本次） | P3-1 PWA：清单 / 图标 / 离线壳 / 新版提示 + 第 9 个套件 `offline-check` |
| `b7271cf` | 修「本地端点上下文」设置误套到 DeepSeek 官方（每次发送都被自己的预检拦下） |
| `0bb08a9` | 夜间日志补齐「待你决定」与提交清单 |
| `5eaa52f` | 布局几何断言 + 状态页数字收尾（viewport 30 → 35） |
| `70a67cf` | 状态页 / 故障排查 / 验收清单 / 隐私说明 + 「没有主动提醒」用例 |
| `88ca596` | 清理误提交的临时文件并忽略 `tmp-commit-*` |
| `ee834f4` | P2-3 检索结果引用：标出来、可点回原话 |
| `a6f249d` | P2-1 发送前预估 + P2-2 输出上限与上下文分开 |
| `1e68687` | 修「导入过的旧对话打不开」+「上次说到」 |
| `6b720d0` | 伴侣模式 P5-1/P5-2（关系档案 / 时间感 / 硬规矩 / 自检） |
| `ba4a63b` | AI 写角色两步化（先扩写提示词，再照稿写卡） |
| `3ff5409` | P1 记忆整理（分组 / 清空 / 上限可调 / 来源可追溯） |
| `12db082` | P0-2~P0-4 数据完整性 / 长对话压力 / 隐私出口自查 |

更早的 P0-1（多视口回归）与配套修复见 `fd275aa` 等提交。

---

## 进度

| 项 | 状态 |
|---|---|
| P0-1 多视口回归 | ✅ 完成 |
| P0-2 存档往返完整性 | ✅ 完成（含隐私修复） |
| P0-3 长对话压力 | ✅ 完成 |
| P0-4 隐私出口自查 | ✅ 完成 |
| P1-1 记忆整理 | ✅ 完成 |
| P1-2 记忆上限可调 | ✅ 完成 |
| P1-3 来源可追溯 | ✅ 完成 |
| 追加：AI 写角色两步化 | ✅ 完成（用户当面要求） |
| 追加：伴侣模式 P5-1/P5-2 | ✅ 完成（含方案文档） |
| 追加：「上次说到」 | ✅ 完成 |
| 追加：修「旧对话打不开」 | ✅ 完成（真缺陷，含两条回归用例） |
| P2-1 发送前预估 | ✅ 完成 |
| P2-2 输出上限与上下文分开 | ✅ 完成 |
| P2-3 检索结果引用 | ✅ 完成 |
| P3-1 PWA（离线壳 + 可安装） | ✅ 完成（新增第 9 个套件 offline-check） |
| P3-2 确认无主动提醒机制 | ✅ 完成（写成扫代码的用例） |
| P3-3 后台状态页 | ✅ 完成（about.html + 机器可读 JSON） |
| P4-1 故障排查手册 | ✅ 完成 |
| P4-2 五阶段验收清单 | ✅ 完成 |
| P4-3 数据与隐私说明 | ✅ 完成（about.html + README） |
| P4-4 夜间日志（本文件） | ✅ 持续更新 |

### P3-2 那条用例当场抓到的问题（记一笔）

1. 我自己刚写的 `about.html` 里出现"连续打卡、好感度"——虽然是在说"我们不做这些"，
   但说明"扫文案"必须区分界面与说明页，于是把说明页排除在外并在注释里写明原因；
2. `archive-ui.js` 里有个 10 秒的 `setTimeout`（收回下载链接用的 URL），
   最初被我写成"≥10 秒就算调度"而误判 —— 阈值改成 5 分钟起步，
   真正要拦的是"定时唤起用户"那种量级。

---

## 早上你只需要做三件事

1. 跑一次 `npm test`（在 `roleworld/` 下，约 4 分钟）——应当 8 个套件全绿、合计 277 项；
2. 打开 `docs/ACCEPTANCE.md` 扫一眼：那是你那张阶段表的逐条核对（含"你自己怎么验"一列），
   以及 `docs/NIGHT_LOG_2026-09-11.md`（本文件）看今晚到底做了什么；
3. 决定要不要发上线（我不会自己发），以及上面「待你决定」里的四件事。

