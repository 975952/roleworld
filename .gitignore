# 私密与本地文件 —— 绝不入库
novel.txt
reports/private/
runs/
*.key
*.pem
.env
.env.*
secrets*.json
*credentials*
api-keys*

# 用户数据（角色卡 / 聊天 / 世界书 / 存档）
data/
profiles/
*.jsonl
*.charx
*.zip
!examples/**/*.json

# 构建产物与依赖
node_modules/
dist/
target/
src-tauri/target/
src-tauri/gen/
build/out/
# 桌面端打包前由 scripts/prepare-desktop.cjs 从 packs/ 复制过来，不入库
app/packs/
*.exe
*.msi
*.dmg
*.AppImage
*.deb
*.apk
*.aab
*.ipa

# 编辑器与系统
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
*.commitmsg.tmp
.commitmsg.tmp
.git-commit-msg.txt
*.cmsg.txt
.cmsg.txt
# 写提交信息用的临时文件（写完就删，别跟着提交进去）
tmp-commit-*.txt
*.cmsg

# 体验卡发卡口令（本机保存，不进仓库）
relay/*.local.txt
# 控制台自己发的卡号留底（等于凭据；上面的 *.jsonl 也覆盖它）
relay/cards.local.jsonl
relay-data/

# 2026-09-18：旧版 local-app-check 的坏字节备份（本机保留、不入库）
# 那个文件本身不是合法 UTF-8（改动前就坏了），而仓库卫生用例会逐文件校验 UTF-8 ——
# 提交它会让 CI 的 adapter-unit 直接红。本地那份**没有删**，只是不进版本库。
tests/local-app-check.pre-replace-20260916.cjs
